# 通讯层重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按顺序完成四项通讯层工作——验证已修的 MQTT 重连、补北向设备状态事件出口、新增南向 TCP/UDP 上报接入、外网 MQTT 安全加固。

**Architecture:** 四个 Phase 顺序执行,每个 Phase 都是独立可交付、可验证的子系统。Phase 1 是对已提交代码 (`a4e2d8f`) 的运行时验证;Phase 2 在既有北向转发服务上并联第三出口;Phase 3 从 MQTT 上行链路提取传输无关的 `UplinkPayloadRouter` 后长出 UDP/TCP 两条南向入口;Phase 4 在内嵌 broker (`MqttServerJob`) 上加认证/加密/ACL。

**Tech Stack:** .NET 10 (net10.0)、SqlSugar ORM (PostgreSQL 6305)、MQTTnet 5.2.0.1603、xunit (IotTests)、Serilog (LogHelper)。

## Global Constraints

- **目标框架**:`net10.0`;后端启动 `dotnet run --project IotWebApi`,实际监听 `http://*:13699`(`Program.cs:38-40` 覆盖 launchSettings)。
- **数据库**:SqlSugar,运行库为 PostgreSQL(`127.0.0.1:6305/zhjngkdb`)。整数列**不要**给 `Length`(PG 会拼出非法 `int(11)`);`ColumnDataType` 用 MySQL 方言(`int`/`varchar`/`text`),`DbContext.cs` 的 `EntityService` 在 PG 下自动改写。
- **CodeFirst 种子铁律**:新实体 + DAO 放进 `IotModel` 程序集即自动纳入迁移;种子**只能**写在 `DAO.override void Init()`,仅表首建时执行一次;禁止把种子写进构造函数或业务方法。
- **MQTTnet 版本**:`5.2.0.1603`(v5 API)。broker 用 `MqttServerFactory().CreateMqttServer(options)`,事件 `+=` 异步委托;拒绝发布 `arg.ProcessPublish = false`,拒绝订阅 `arg.ProcessSubscription = false`。
- **对抗性验证铁律**:`IotSimulator`/`Sim/*Slave.cs` 的从站编解码独立实现,**一行都不许动**。
- **日志**:`LogHelper.SysLogWrite`(Information,进全量 `Logs/zhjngk-webapi-{date}.log`)/ `LogHelper.ErrorLogWrite`(进 `-error-` 文件)。
- **提交纪律**:每个 Task 末尾 commit(Conventional Commits,中文,无 AI 署名);**push 需用户单独授权**。
- **测试**:`dotnet test IotTests/IotTests.csproj`(xunit)。
- **设备状态语义**(权威 `IotModel/MEntity/Device/DeviceInfo.cs:92-96`):`2=在线`、`1=掉电`、`0=离线`。
- **deviceKey 约定**:deviceKey 主匹配 `DeviceInfo.DeviceGateway`,纯数字兜底 `DeviceId`(`ProtocolScriptService.cs:143-158`)。

---

## Phase 1 — 验证 MQTT 重连修复(人工端到端)

> 背景:`MqttClientJob.cs` 的重连已在 `a4e2d8f` 修复(固定 ClientId、`SemaphoreSlim` 串行、释放旧 client、`await` 不吞异常)。编译已过。本 Phase 验证运行时行为:**重连后单连接、日志不翻倍、入库不重复**。
>
> ⚠ **此 Phase 无法自动化**:上行客户端连的是外部 broker `192.168.0.76:1883`(实际值以 DB 表 `admin_mqttparam` 为准),制造断线需 broker 管理权限。以下为**人工验证清单**,不是自动化测试。执行者需具备:后端本地运行环境(PG 6305 / Redis 8111 可达)、对该 broker 的管理端或网络阻断能力。

### Task 1.1: 静态自检——确认改动已在源码中

**Files:**
- Read: `IotWebApi/Services/Jobs/MqttClientJob.cs`

- [ ] **Step 1: 确认四处修复在位**

逐条核对(行号以当前文件为准):
- `.WithClientId(mqttname)` —— 固定 ClientId(不再含 `SnowModel.Instance.NewId()`)
- `private static readonly SemaphoreSlim _initLock` 字段存在;`TryInitializeClient` 开头 `await _initLock.WaitAsync();`,`finally` 中 `_initLock.Release();`
- `TryInitializeClient` 进门有幂等 `if (... IsConnected) return true;`,建新 client 前有 `old.DisconnectAsync()` + `old.Dispose()`
- `DisconnectedHandler` 返回 `Task`,重连体在 `Task.Run(async () => { await Task.Delay(20000); await TryInitializeClient(); })`

- [ ] **Step 2: 确认编译通过**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo`
Expected: `0 个错误`。

### Task 1.2: 人工端到端验证清单

- [ ] **Step 1: 确认 DB 当前 broker 配置**

查 `admin_mqttparam` 表实际 `mqtt_server` / `mqtt_client_port` / `mqtt_sub_topic_web_api`(播种默认 `192.168.0.76` / `1883` / `zhjngk/receive/webapi`,但可能已被改)。记录实际值。

- [ ] **Step 2: 启动后端,确认首连成功**

Run: `dotnet run --project IotWebApi`
观察 `Logs/zhjngk-webapi-{今日日期}.log`,应依次出现一次:
```
尝试初始化MQTT客户端
MQTT客户端初始化成功
webapi服务端的连接成功并订阅
```
（`webapi服务端的连接成功并订阅` = `MqttClientJob.cs:167`，代表恰好一次订阅。）

- [ ] **Step 2.5: 确认 broker 侧只有一条 `IotWebApi_Main` 连接**

在 broker 管理端查连接列表,ClientId=`IotWebApi_Main` 应为 **1 条**。

- [ ] **Step 3: 制造一次断线**

从 broker 侧断开(重启 broker / 防火墙阻断到 `192.168.0.76:1883` / 管理端踢掉 `IotWebApi_Main`)。**不要**靠重启内嵌 broker(13386)——上行客户端不连它,且它默认禁用(`JobInitializer.cs:25` JobStatus=0)。

- [ ] **Step 4: 观察重连,判定无翻倍**

断线后日志应出现:`MQTT客户端连接断开，原因: ...`;约 20 秒后出现**恰好一组** `尝试初始化MQTT客户端`→`MQTT客户端初始化成功`→`webapi服务端的连接成功并订阅`。
**判定标准(全部满足才算通过)**:
1. broker 管理端 `IotWebApi_Main` 仍只有 **1 条**活跃连接(同名顶替,旧连接被踢)。
2. 日志中**没有**并发多组初始化(信号量幂等生效)。
3. 恢复后向 `{mqtt_sub_topic_web_api}/<某deviceKey>` 发 N 条唯一载荷,`Mqtt接收数据：`(`MqttClientJob.cs:200`,需 `MqttConfig:LogOpen=true`,当前为 true)出现次数 = **N**(非 2N),对应入库条数 = **N**。

- [ ] **Step 5: 记录验证结论**

把验证结果(通过/未通过 + 现象)追加到本文件 Phase 1 末尾,或记入 commit message。本 Phase **无代码改动、无 commit**(除非 Step 4 暴露新问题需修复,那时另起 bugfix task)。

---

## Phase 2 — 北向设备状态事件出口

> 目标:在 `NorthboundForwardService` 并联第三出口 `ForwardDeviceState`,让第三方收到可靠的设备上线/离线/掉电事件,而非从告警流里靠字符串推断。参考方案 `docs/北向设备状态事件方案.md`。

### 文件结构

- Modify: `IotWebApi/Services/NorthboundForwardService.cs` —— 加 `ForwardDeviceState` 入口 + `SinkWorker.AcceptDeviceState`
- Modify: `IotWebApi/Services/DataPointIngestService.cs` —— 上线(:477 区)、离线确认(`ConfirmOffline`)两处并联接线
- Test: `IotTests/NorthboundDeviceStateTests.cs`(新建)

### Task 2.1: NorthboundForwardService 新增 ForwardDeviceState 出口

**Files:**
- Modify: `IotWebApi/Services/NorthboundForwardService.cs`
- Test: `IotTests/NorthboundDeviceStateTests.cs`

**Interfaces:**
- Produces:
  - `public void ForwardDeviceState(int deviceid, string typecode, int devicestate, string reason)`
  - `SinkWorker.AcceptDeviceState(int deviceid, int devicestate, string reason)`(private 内部类方法)
  - 北向报文格式:`{ msgType:"deviceState", deviceId, deviceState, reason }`

- [ ] **Step 1: 在 `#region 转发入口`(约 L129-167)内新增入口方法**

紧挨 `ForwardAlarm`(L148-157)之后加入。与 `ForwardAlarm` 同构:遍历 `_workers.Values`,`ContentMode == 1`(仅遥测)时跳过,`MatchScope` 过闸后调 `AcceptDeviceState`。

```csharp
/// <summary>
/// 北向设备状态事件出口(与遥测/告警并列的第三出口):上线/离线/掉电作为客观事实转发,
/// 不查告警字典、不进告警生命周期、不受屏蔽规则影响。devicestate: 2=在线,1=掉电,0=离线。
/// </summary>
public void ForwardDeviceState(int deviceid, string typecode, int devicestate, string reason)
{
    if (_workers.IsEmpty) return;
    foreach (var worker in _workers.Values)
    {
        if (worker.Sink.ContentMode == 1) continue;              // 1=仅遥测,状态事件属事件类,跳过
        if (!worker.MatchScope(deviceid, typecode)) continue;
        worker.AcceptDeviceState(deviceid, devicestate, reason);
    }
}
```

- [ ] **Step 2: 在内部类 `SinkWorker` 中新增 `AcceptDeviceState`(紧挨 `AcceptAlarm` L402-409 之后)**

```csharp
public void AcceptDeviceState(int deviceid, int devicestate, string reason)
{
    Enqueue(new ForwardItem
    {
        Topic = _eventTopic.Replace("{deviceId}", deviceid.ToString()),
        Payload = new { msgType = "deviceState", deviceId = deviceid, deviceState = devicestate, reason }.ToJson()
    });
}
```
（复用 `AcceptAlarm` 同款 `_eventTopic` 与 `Enqueue`——落盘重传、多目的地分发链路全部自动复用。）

- [ ] **Step 3: 写单测——验证报文格式与三态**

`IotTests/NorthboundDeviceStateTests.cs`。因 `SinkWorker`/`_workers` 为 private，单测聚焦**报文序列化契约**(第三方消费依赖的字段名与三态取值),用与生产同款的匿名对象断言 JSON 形态：

```csharp
using Xunit;

namespace IotTests
{
    public class NorthboundDeviceStateTests
    {
        [Theory]
        [InlineData(2, "在线")]
        [InlineData(1, "掉电")]
        [InlineData(0, "离线")]
        public void DeviceStatePayload_carries_three_states(int state, string _)
        {
            // 与 SinkWorker.AcceptDeviceState 完全一致的报文构造
            var payload = new { msgType = "deviceState", deviceId = 123, deviceState = state, reason = "无数据采集" }.ToJson();

            Assert.Contains("\"msgType\":\"deviceState\"", payload);
            Assert.Contains($"\"deviceState\":{state}", payload);
            Assert.Contains("\"deviceId\":123", payload);
        }
    }
}
```
（`.ToJson()` 为本仓扩展方法，`IotTests` 已引用 `CenBoCommon.Zxx`；若命名空间缺失，在文件顶部补 `using CenBoCommon.Zxx;`。）

- [ ] **Step 4: 跑测试**

Run: `dotnet test IotTests/IotTests.csproj --filter NorthboundDeviceStateTests`
Expected: 3 passed。

- [ ] **Step 5: 提交**

```bash
git add IotWebApi/Services/NorthboundForwardService.cs IotTests/NorthboundDeviceStateTests.cs
git commit -m "feat(northbound): 新增 ForwardDeviceState 设备状态事件出口"
```

### Task 2.2: DataPointIngestService 两处接线

**Files:**
- Modify: `IotWebApi/Services/DataPointIngestService.cs`

**Interfaces:**
- Consumes: `_northboundForwardService.ForwardDeviceState(int, string, int, string)`(Task 2.1);字段 `_northboundForwardService` 已注入(L106)。

- [ ] **Step 1: 上线接线(:477 区,`data`/`dbdev` 均可见)**

在 `_alarmEngineService.FireOfflineRecover(data.DeviceId);`(L477)之后并联一行:

```csharp
_alarmEngineService.FireOfflineRecover(data.DeviceId);
_northboundForwardService.ForwardDeviceState(data.DeviceId, data.device.DeviceTypeCode, 2, "通信恢复上线");   // 新增:上线事实,状态=2
```

- [ ] **Step 2: 离线确认接线(`ConfirmOffline`,改在 confirm 循环内——避开 :547 的元组缺字段坑)**

⚠ `offlinefires` 元组只有 `(DeviceId, Reason)`,在 L545 的 `fire` 循环里拿不到 `DeviceState`/`typecode`。**改在 confirm 循环内(L519-536,`dbdev` 与 `confirm` 均可见)接线**,不动 `offlinefires` 结构:

在 `confirm` 循环中、`dbdev.DeviceState = confirm.DeviceState;` 落库之后,`offlinefires.Add(...)` 附近加入:

```csharp
dbdev.DeviceState = confirm.DeviceState;
deviceupdates.Add(dbdev);
offlinefires.Add((confirm.DeviceId, confirm.Reason));
_northboundForwardService.ForwardDeviceState(confirm.DeviceId, dbdev.DeviceTypeCode, confirm.DeviceState, confirm.Reason);   // 新增:离线/掉电事实,状态=confirm.DeviceState(0或1)
```
（`confirm.DeviceState` 即目标状态,离线场景为 0 或 1;`dbdev.DeviceTypeCode` 是此上下文可见的 typecode。）

- [ ] **Step 3: 编译验证**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo`
Expected: `0 个错误`。

- [ ] **Step 4: 提交**

```bash
git add IotWebApi/Services/DataPointIngestService.cs
git commit -m "feat(northbound): 上线与离线确认两处并联设备状态事件出口"
```

### Task 2.3: 集成回归(人工/半自动)

- [ ] **Step 1: 回归断言(记入验证记录,不阻塞交付)**

启动后端,验证三条(参考 `docs/北向设备状态事件方案.md` §7):
1. 设备停止上报 → 超确认时长 → 第三方收到 `deviceState=0/1`。
2. 设备恢复上报 → 第三方**立即**收到 `deviceState=2`。
3. **屏蔽离线告警后**,上述两条仍成立(核心断言:新出口不进告警生命周期)。

现有离线告警/恢复行为**逐字不变**(本 Phase 只并联,不改告警链路)。

---

## Phase 3 — 南向 TCP/UDP 上报接入

> 目标:提取传输无关的 `UplinkPayloadRouter`,让 UDP/TCP 复用同一条上行管道。参考方案 `docs/南向TCP-UDP上报接入方案.md`。设备身份靠来源 IP 匹配 `DeviceInfo.DeviceIp`。

### 文件结构

- Create: `IotWebApi/Services/Uplink/UplinkPayloadRouter.cs` —— 三层契约解析(传输无关)
- Create: `IotWebApi/Services/Uplink/UdpUplinkListener.cs` —— UDP 监听(IHostedService)
- Create: `IotWebApi/Services/Uplink/TcpUplinkListener.cs` —— TCP 监听(IHostedService,持有 TcpServerChannel)
- Modify: `IotWebApi/Services/Jobs/MqttClientJob.cs` —— `MessageReceivedHandler` 改调 UplinkPayloadRouter
- Modify: `IotWebApi/Services/ProtocolScriptService.cs` —— `DecodeMqttPayload` → `DecodePayload`
- Modify: `IotWebApi/Services/Mqtt/MqttClientService.cs` —— 新增 `TcpUplinkPluginGuid`/`UdpUplinkPluginGuid` 常量
- Modify: `IotWebApi/Program.cs` —— 注册两个 HostedService + 静态桥
- Test: `IotTests/UplinkPayloadRouterTests.cs`(新建)

### Task 3.1: DecodeMqttPayload 改名为 DecodePayload

**Files:**
- Modify: `IotWebApi/Services/ProtocolScriptService.cs:148`
- Modify: `IotWebApi/Services/Jobs/MqttClientJob.cs`(调用点)

**Interfaces:**
- Produces: `public List<DeviceData> DecodePayload(string devicekey, byte[] payload)`(原 `DecodeMqttPayload`,逻辑零改动)

- [ ] **Step 1: 重命名方法**

`ProtocolScriptService.cs:148`:`public List<DeviceData> DecodeMqttPayload(string devicekey, byte[] payload)` → `public List<DeviceData> DecodePayload(string devicekey, byte[] payload)`。函数体不动。

- [ ] **Step 2: 更新唯一调用点**

`MqttClientJob.cs`(约 L240)`MqttClientService.ScriptService?.DecodeMqttPayload(devicekey, buffer)` → `DecodePayload(devicekey, buffer)`。（Step 3.2 会把这行整体搬进 Router，此处先保证改名后可编译。）

- [ ] **Step 3: 编译**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo`
Expected: `0 个错误`。

- [ ] **Step 4: 提交**

```bash
git add IotWebApi/Services/ProtocolScriptService.cs IotWebApi/Services/Jobs/MqttClientJob.cs
git commit -m "refactor(uplink): DecodeMqttPayload 改名 DecodePayload（去除 MQTT 语义）"
```

### Task 3.2: 提取 UplinkPayloadRouter(纯重构,MQTT 必须回归)

**Files:**
- Create: `IotWebApi/Services/Uplink/UplinkPayloadRouter.cs`
- Modify: `IotWebApi/Services/Jobs/MqttClientJob.cs`
- Test: `IotTests/UplinkPayloadRouterTests.cs`

**Interfaces:**
- Produces: `public static PluginMessage? UplinkPayloadRouter.Route(string devicekey, byte[] buffer)` —— 返回 null 表示无法识别,调用方丢弃并记日志。三层契约:① `PluginMessage` 原样;② 裸 `List<DeviceData>` 包成 `协议解析`;③ 脚本解码。
- Consumes: `MqttClientService.ScriptService.DecodePayload`(Task 3.1)

- [ ] **Step 1: 写失败测试**

`IotTests/UplinkPayloadRouterTests.cs`:

```csharp
using System.Text;
using Xunit;
using IotWebApi.Services.Uplink;
using CenboEventBus;

namespace IotTests
{
    public class UplinkPayloadRouterTests
    {
        [Fact]
        public void Route_contract1_PluginMessage_passthrough()
        {
            var json = "{\"MessageType\":2,\"MessageJson\":\"[{\\\"DeviceId\\\":1}]\"}";
            var msg = UplinkPayloadRouter.Route("dev1", Encoding.UTF8.GetBytes(json));
            Assert.NotNull(msg);
            Assert.Equal(PluginMessageEnum.协议解析, msg!.MessageType);
        }

        [Fact]
        public void Route_contract2_bareDeviceDataList_wrapped()
        {
            var json = "[{\"DeviceId\":5,\"DeviceState\":2}]";
            var msg = UplinkPayloadRouter.Route("dev1", Encoding.UTF8.GetBytes(json));
            Assert.NotNull(msg);
            Assert.Equal(PluginMessageEnum.协议解析, msg!.MessageType);
        }

        [Fact]
        public void Route_unrecognized_returns_null()
        {
            var msg = UplinkPayloadRouter.Route("dev1", new byte[] { 0x01, 0x02, 0x03 });
            // 无脚本设备时脚本解码返回空 → Route 返回 null
            Assert.Null(msg);
        }
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test IotTests/IotTests.csproj --filter UplinkPayloadRouterTests`
Expected: FAIL（`UplinkPayloadRouter` 不存在,编译错误）。

- [ ] **Step 3: 创建 Router(把 MqttClientJob 三层契约逐字搬来)**

`IotWebApi/Services/Uplink/UplinkPayloadRouter.cs`:

```csharp
using System.Text;
using CenboEventBus;
using CenBoCommon.Zxx;
using IotModel;
using IotWebApi.Services.Mqtt;

namespace IotWebApi.Services.Uplink
{
    /// <summary>
    /// 上行载荷三层契约解析(传输无关,MQTT/TCP/UDP 共用)。
    /// 返回 null 表示无法识别,调用方应丢弃并记日志。
    /// </summary>
    public static class UplinkPayloadRouter
    {
        public static PluginMessage? Route(string devicekey, byte[] buffer)
        {
            if (buffer == null || buffer.Length == 0) return null;
            string strdata = Encoding.UTF8.GetString(buffer);

            PluginMessage? message = null;
            // 契约① PluginMessage{MessageType,MessageJson} 原样路由
            try
            {
                var wrapped = strdata.ToObject<PluginMessage>();
                if (wrapped != null && !wrapped.MessageJson.IsZxxNullOrEmpty()) message = wrapped;
            }
            catch { }
            // 契约② 裸 List<DeviceData> 默认按协议解析包装
            if (message == null)
            {
                try
                {
                    var datalist = strdata.ToObject<List<DeviceData>>();
                    if (datalist.IsZxxAny() && datalist.Exists(t => t.DeviceId > 0))
                        message = new PluginMessage { MessageType = PluginMessageEnum.协议解析, MessageJson = strdata };
                }
                catch { }
            }
            // 契约③ 兜底:非JSON载荷按产品挂JS脚本解码
            if (message == null)
            {
                var scriptdata = MqttClientService.ScriptService?.DecodePayload(devicekey, buffer);
                if (scriptdata.IsZxxAny())
                    message = new PluginMessage { MessageType = PluginMessageEnum.协议解析, MessageJson = scriptdata.ToJson() };
            }
            return message;
        }
    }
}
```

- [ ] **Step 4: MqttClientJob.MessageReceivedHandler 改调 Router(行为逐字不变)**

把 `MessageReceivedHandler` 中三层契约那段(约 L202-253)替换为:

```csharp
var bus = MqttClientService.EventBus;
if (bus == null)
{
    LogHelper.SysLogWrite("MqttClientJob", "MessageReceivedHandler", "事件总线未就绪，MQTT上行消息已丢弃。", mqttname);
    return Task.CompletedTask;
}
var segments = args.ApplicationMessage.Topic.Split('/', StringSplitOptions.RemoveEmptyEntries);
string devicekey = segments.Length > 0 ? segments[^1] : "";
var message = IotWebApi.Services.Uplink.UplinkPayloadRouter.Route(devicekey, buffer);
if (message == null)
{
    LogHelper.SysLogWrite("MqttClientJob", "MessageReceivedHandler",
        $"无法识别的MQTT上行载荷，已忽略：{strdata[..Math.Min(200, strdata.Length)]}", mqttname);
    return Task.CompletedTask;
}
bus.Publish(new PluginEvent(MqttClientService.MqttPluginGuid, message));
```

- [ ] **Step 5: 跑测试确认通过 + MQTT 编译回归**

Run: `dotnet test IotTests/IotTests.csproj --filter UplinkPayloadRouterTests`
Expected: 3 passed。
Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo`
Expected: `0 个错误`。

- [ ] **Step 6: 提交**

```bash
git add IotWebApi/Services/Uplink/UplinkPayloadRouter.cs IotWebApi/Services/Jobs/MqttClientJob.cs IotTests/UplinkPayloadRouterTests.cs
git commit -m "refactor(uplink): 提取 UplinkPayloadRouter，MQTT 上行改用统一路由"
```

### Task 3.3: 虚拟插件 GUID 常量

**Files:**
- Modify: `IotWebApi/Services/Mqtt/MqttClientService.cs`

**Interfaces:**
- Produces: `public const string TcpUplinkPluginGuid`、`public const string UdpUplinkPluginGuid`

- [ ] **Step 1: 加两个常量(紧挨 `MqttPluginGuid` L22)**

```csharp
public const string MqttPluginGuid = "0af3b6c9d2e5081b4a7c0d3e6f9a2b5c";
public const string TcpUplinkPluginGuid = "1b0c4d7e2f6a093c5b8d1e4f7a0b3c6d";   // TCP 南向上行溯源
public const string UdpUplinkPluginGuid = "2c1d5e8f3a7b0a4d6c9e2f5a8b1c4d7e";   // UDP 南向上行溯源
```

- [ ] **Step 2: 提交(与 3.4/3.5 一起 build,先落常量)**

```bash
git add IotWebApi/Services/Mqtt/MqttClientService.cs
git commit -m "feat(uplink): 新增 TCP/UDP 上行虚拟插件 GUID 常量"
```

### Task 3.4: UDP 监听器

**Files:**
- Create: `IotWebApi/Services/Uplink/UdpUplinkListener.cs`
- Modify: `IotWebApi/Program.cs`

**Interfaces:**
- Consumes: `UplinkPayloadRouter.Route`、`DeviceInfoDAO.Instance.GetOneBy`、`MqttClientService.EventBus`、`MqttClientService.UdpUplinkPluginGuid`
- Produces: `public class UdpUplinkListener : IHostedService`;监听端口取自配置 `Uplink:UdpPort`(默认 0 = 不启用)。

- [ ] **Step 1: 创建 UDP 监听器**

`IotWebApi/Services/Uplink/UdpUplinkListener.cs`:

```csharp
using System.Net;
using System.Net.Sockets;
using CenboEventBus;
using IotLog;
using IotModel;
using IotWebApi.Services.Mqtt;

namespace IotWebApi.Services.Uplink
{
    /// <summary>
    /// UDP 南向上报监听器:每个数据报即一帧(天然定界),来源IP匹配 DeviceInfo.DeviceIp。
    /// 只接受已登记 IP(白名单),未登记的包直接丢。设备主动上报,不实现 Send。
    /// </summary>
    public class UdpUplinkListener : IHostedService
    {
        private UdpClient? _udp;
        private CancellationTokenSource? _cts;
        private readonly int _port;

        public UdpUplinkListener()
        {
            _port = int.TryParse(AppSetting.GetConfig("Uplink:UdpPort"), out var p) ? p : 0;
        }

        public Task StartAsync(CancellationToken cancellationToken)
        {
            if (_port <= 0) return Task.CompletedTask;   // 未配置端口=不启用
            _cts = new CancellationTokenSource();
            _udp = new UdpClient(new IPEndPoint(IPAddress.Parse(AppSetting.GetConfig("Uplink:BindAddress") ?? "0.0.0.0"), _port));
            _ = Task.Run(() => ReceiveLoop(_cts.Token));
            LogHelper.SysLogWrite("UdpUplinkListener", "StartAsync", $"UDP 南向监听已启动:端口 {_port}", "UdpUplink");
            return Task.CompletedTask;
        }

        private async Task ReceiveLoop(CancellationToken token)
        {
            while (!token.IsCancellationRequested && _udp != null)
            {
                try
                {
                    var result = await _udp.ReceiveAsync(token);
                    string ip = result.RemoteEndPoint.Address.MapToIPv4().ToString();
                    var device = DeviceInfoDAO.Instance.GetOneBy(t => t.DeviceIp == ip);
                    if (device == null)   // IP 白名单:未登记来源直接丢
                    {
                        LogHelper.SysLogWrite("UdpUplinkListener", "ReceiveLoop", $"丢弃未登记来源 UDP 包:{ip}", "UdpUplink");
                        continue;
                    }
                    var message = UplinkPayloadRouter.Route(device.DeviceGateway ?? device.DeviceId.ToString(), result.Buffer);
                    if (message != null)
                        MqttClientService.EventBus?.Publish(new PluginEvent(MqttClientService.UdpUplinkPluginGuid, message));
                }
                catch (OperationCanceledException) { break; }
                catch (Exception ex)
                {
                    LogHelper.ErrorLogWrite("UdpUplinkListener", "ReceiveLoop", ex.ToString(), "UdpUplink");
                }
            }
        }

        public Task StopAsync(CancellationToken cancellationToken)
        {
            _cts?.Cancel();
            _udp?.Dispose();
            return Task.CompletedTask;
        }
    }
}
```

- [ ] **Step 2: 注册 HostedService(Program.cs,照 NorthboundForwardService L265-266 两行模式)**

```csharp
builder.Services.AddSingleton<IotWebApi.Services.Uplink.UdpUplinkListener>();
builder.Services.AddHostedService(sp => sp.GetRequiredService<IotWebApi.Services.Uplink.UdpUplinkListener>());
```

- [ ] **Step 3: 编译**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo`
Expected: `0 个错误`。

- [ ] **Step 4: 提交**

```bash
git add IotWebApi/Services/Uplink/UdpUplinkListener.cs IotWebApi/Program.cs
git commit -m "feat(uplink): UDP 南向上报监听器（来源IP白名单+统一路由）"
```

### Task 3.5: TCP 监听器(复用 TcpServerChannel + splitFrames/换行定界)

**Files:**
- Create: `IotWebApi/Services/Uplink/TcpUplinkListener.cs`
- Modify: `IotWebApi/Program.cs`

**Interfaces:**
- Consumes: `TcpServerChannel(int port)`、`.EndpointResolver`、`.FrameReceived`、`.SessionClosed`、`FrameAccumulator`、`ProtocolScriptService.RunSplitFrames`、`UplinkPayloadRouter.Route`
- Produces: `public class TcpUplinkListener : IHostedService`;端口取自 `Uplink:TcpPort`(默认 0)。

- [ ] **Step 1: 创建 TCP 监听器(第一版按换行 `\n` 定界;splitFrames 见 Step 2 注释)**

`IotWebApi/Services/Uplink/TcpUplinkListener.cs`:

```csharp
using CenboEventBus;
using IotDriverCore.Channels;
using IotLog;
using IotModel;
using IotWebApi.Services.Mqtt;

namespace IotWebApi.Services.Uplink
{
    /// <summary>
    /// TCP 南向上报监听器:复用 TcpServerChannel(连接管理/心跳/空闲踢连接现成),
    /// 来源IP 经 EndpointResolver 映射到 deviceKey。字节流按换行符 '\n' 定界。
    /// 只接受已登记 IP,未登记连接由 EndpointResolver 返回 null → 走 "IP:Port" 兜底端点,
    /// 该端点查不到设备则丢帧。
    /// </summary>
    public class TcpUplinkListener : IHostedService
    {
        private TcpServerChannel? _channel;
        private readonly int _port;
        // 换行定界:提取器找第一个 '\n',返回 [0, idx+1]
        private readonly FrameAccumulator _acc = new FrameAccumulator(buf =>
        {
            int idx = System.Array.IndexOf(buf, (byte)'\n');
            return idx < 0 ? (-1, 0) : (0, idx + 1);
        });

        public TcpUplinkListener()
        {
            _port = int.TryParse(AppSetting.GetConfig("Uplink:TcpPort"), out var p) ? p : 0;
        }

        public Task StartAsync(CancellationToken cancellationToken)
        {
            if (_port <= 0) return Task.CompletedTask;
            _channel = new TcpServerChannel(_port)
            {
                IdleTimeoutSeconds = 300,
                EndpointResolver = ip =>
                {
                    var device = DeviceInfoDAO.Instance.GetOneBy(t => t.DeviceIp == ip);
                    return device?.DeviceGateway ?? device?.DeviceId.ToString();   // 返回 deviceKey;null → 兜底 IP:Port
                }
            };
            _channel.FrameReceived = OnFrame;
            _channel.SessionClosed = ep => _acc.Reset(ep);
            _channel.Start();
            LogHelper.SysLogWrite("TcpUplinkListener", "StartAsync", $"TCP 南向监听已启动:端口 {_port}", "TcpUplink");
            return Task.CompletedTask;
        }

        private void OnFrame(string endpoint, byte[] raw)
        {
            // endpoint 即 EndpointResolver 返回的 deviceKey(未登记时为 "IP:Port")
            foreach (var frame in _acc.Push(endpoint, raw))
            {
                var message = UplinkPayloadRouter.Route(endpoint, frame);
                if (message != null)
                    MqttClientService.EventBus?.Publish(new PluginEvent(MqttClientService.TcpUplinkPluginGuid, message));
                else
                    LogHelper.SysLogWrite("TcpUplinkListener", "OnFrame", $"无法识别的TCP上行帧,来源端点={endpoint}", "TcpUplink");
            }
        }

        public Task StopAsync(CancellationToken cancellationToken)
        {
            _channel?.Stop();
            _channel?.Dispose();
            return Task.CompletedTask;
        }
    }
}
```
> 注:脚本 `splitFrames` 定界(方案 §6.2)为增量项。第一版按 `\n` 定界满足 JSON 设备;挂脚本设备的 `splitFrames` 接线需调 `ProtocolScriptService.RunSplitFrames` 并解析返回的 `ResultJson`(`{frames,consumed}`),待第一版跑通后再加,避免一次引入过多不确定性。

- [ ] **Step 2: 注册 HostedService(Program.cs)**

```csharp
builder.Services.AddSingleton<IotWebApi.Services.Uplink.TcpUplinkListener>();
builder.Services.AddHostedService(sp => sp.GetRequiredService<IotWebApi.Services.Uplink.TcpUplinkListener>());
```

- [ ] **Step 3: 编译**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo`
Expected: `0 个错误`。

- [ ] **Step 4: 提交**

```bash
git add IotWebApi/Services/Uplink/TcpUplinkListener.cs IotWebApi/Program.cs
git commit -m "feat(uplink): TCP 南向上报监听器（复用 TcpServerChannel + 换行定界）"
```

### Task 3.6: 单测——换行定界拆帧

**Files:**
- Test: `IotTests/TcpFrameSplitTests.cs`

- [ ] **Step 1: 写测试(半包/粘包/多帧)**

```csharp
using System.Text;
using Xunit;
using IotDriverCore.Channels;

namespace IotTests
{
    public class TcpFrameSplitTests
    {
        private static FrameAccumulator NewlineAcc() => new FrameAccumulator(buf =>
        {
            int idx = System.Array.IndexOf(buf, (byte)'\n');
            return idx < 0 ? (-1, 0) : (0, idx + 1);
        });

        [Fact]
        public void Splits_stuck_and_half_packets()
        {
            var acc = NewlineAcc();
            var f1 = acc.Push("ep", Encoding.UTF8.GetBytes("aaa\nbbb"));   // 一整帧 + 半帧
            Assert.Single(f1);
            Assert.Equal("aaa\n", Encoding.UTF8.GetString(f1[0]));

            var f2 = acc.Push("ep", Encoding.UTF8.GetBytes("ccc\nddd\n")); // 补齐 bbbccc + 一整帧
            Assert.Equal(2, f2.Count);
            Assert.Equal("bbbccc\n", Encoding.UTF8.GetString(f2[0]));
            Assert.Equal("ddd\n", Encoding.UTF8.GetString(f2[1]));
        }
    }
}
```

- [ ] **Step 2: 跑测试**

Run: `dotnet test IotTests/IotTests.csproj --filter TcpFrameSplitTests`
Expected: passed。

- [ ] **Step 3: 提交**

```bash
git add IotTests/TcpFrameSplitTests.cs
git commit -m "test(uplink): TCP 换行定界半包/粘包拆帧单测"
```

---

## Phase 4 — 外网 MQTT 安全接入

> 目标:内嵌 broker (`MqttServerJob`) 补齐认证/加密/ACL。终稿见 `docs/外网MQTT安全接入方案.md`:每设备 token + 服务端单向 TLS(自签+客户端跳过链校验)+ Topic ACL,客户端 X.509 永久排除。
>
> ⚠ **迁移纪律**:每个 Task 独立可发布,**任何一步都不破坏 1883 存量链路**。落地前不得暴露任何 MQTT 端口到外网。

### 文件结构

- Create: `IotModel/MEntity/Basic/DeviceMqttCredential.cs` —— 凭据实体
- Create: `IotModel/MDAO/Basic/DeviceMqttCredentialDAO.cs` —— DAO(单例+种子)
- Modify: `IotWebApi/Common/EncryptsHelper.cs` —— 加 PBKDF2 静态方法
- Create: `IotWebApi/Services/Mqtt/MqttCredentialCache.cs` —— L1(内存)+L2(Redis)认证缓存
- Modify: `IotWebApi/Services/Jobs/MqttServerJob.cs` —— 内网口收窄 + 8883 TLS + 认证改造 + ACL
- Test: `IotTests/MqttSecurityTests.cs`(新建)

### Task 4.1: 内网口收窄绑定(独立可发布,零依赖)

**Files:**
- Modify: `IotWebApi/Services/Jobs/MqttServerJob.cs:111`
- Modify: `IotModel/MEntity/Basic/AdminMqttparam.cs`(加 `LanBindAddress` 字段)

- [ ] **Step 1: AdminMqttparam 加可空绑定地址字段(紧挨 MqttServerPort L48 后)**

```csharp
[SugarColumn(ColumnName = "lan_bind_address", Length = 64, IsNullable = true, ColumnDataType = "varchar", DefaultValue = "0.0.0.0")]
public string LanBindAddress { get; set; }
```
> 注:改 `admin_mqttparam` 表须重启后端才生效(既有行为)。新列由 CodeFirst 自动加(SqlSugar `InitTables` 对已存在表补列)。

- [ ] **Step 2: MqttServerJob 收窄默认端点绑定(L111)**

```csharp
// 改前:optionsBuilder.WithDefaultEndpointBoundIPAddress(IPAddress.Any);
var lanbind = string.IsNullOrWhiteSpace(MqttParam.LanBindAddress) ? "0.0.0.0" : MqttParam.LanBindAddress;
optionsBuilder.WithDefaultEndpointBoundIPAddress(IPAddress.Parse(lanbind));
```

- [ ] **Step 3: 编译 + 提交**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo` → `0 个错误`
```bash
git add IotModel/MEntity/Basic/AdminMqttparam.cs IotWebApi/Services/Jobs/MqttServerJob.cs
git commit -m "feat(mqtt-sec): 内网口可配置收窄绑定（LanBindAddress）"
```

### Task 4.2: 凭据实体 + DAO + PBKDF2

**Files:**
- Create: `IotModel/MEntity/Basic/DeviceMqttCredential.cs`
- Create: `IotModel/MDAO/Basic/DeviceMqttCredentialDAO.cs`
- Modify: `IotWebApi/Common/EncryptsHelper.cs`
- Test: `IotTests/MqttSecurityTests.cs`

**Interfaces:**
- Produces:
  - `DeviceMqttCredential { int Id; string MqttUser; string PassHash; string Salt; bool IsEnable; int TenantId; }`
  - `DeviceMqttCredentialDAO.Instance`
  - `EncryptsHelper.Pbkdf2Hash(string password, out string saltBase64) : string`(返回 hash base64)
  - `EncryptsHelper.Pbkdf2Verify(string password, string saltBase64, string hashBase64) : bool`

- [ ] **Step 1: 创建实体(注意 CodeFirst 铁律:整数列不给 Length)**

`IotModel/MEntity/Basic/DeviceMqttCredential.cs`:

```csharp
using SqlSugar;
using System.ComponentModel;

namespace IotModel
{
    [DisplayName("设备MQTT凭据")]
    [SugarTable(TableName = "device_mqtt_credential", TableDescription = "设备MQTT凭据", IsDisabledUpdateAll = true)]
    public class DeviceMqttCredential : ITenantEntity
    {
        [SugarColumn(ColumnName = "id", IsPrimaryKey = true, IsIdentity = true, ColumnDataType = "int", DefaultValue = "0")]
        public int Id { get; set; }

        [SugarColumn(ColumnName = "mqtt_user", Length = 64, IsNullable = true, ColumnDataType = "varchar", DefaultValue = "")]
        public string MqttUser { get; set; }

        [SugarColumn(ColumnName = "pass_hash", Length = 200, IsNullable = true, ColumnDataType = "varchar", DefaultValue = "")]
        public string PassHash { get; set; }

        [SugarColumn(ColumnName = "salt", Length = 64, IsNullable = true, ColumnDataType = "varchar", DefaultValue = "")]
        public string Salt { get; set; }

        [SugarColumn(ColumnName = "device_gateway", Length = 30, IsNullable = true, ColumnDataType = "varchar", DefaultValue = "")]
        public string DeviceGateway { get; set; }   // 绑定设备(=deviceKey),供 ACL 校验

        [SugarColumn(ColumnName = "is_enable", ColumnDataType = "tinyint", DefaultValue = "1")]
        public bool IsEnable { get; set; }

        [SugarColumn(ColumnName = "tenant_id", ColumnDataType = "int", DefaultValue = "0")]
        public int TenantId { get; set; }
    }
}
```

- [ ] **Step 2: 创建 DAO(单例;Init 留空——凭据由业务签发,不种子)**

`IotModel/MDAO/Basic/DeviceMqttCredentialDAO.cs`:

```csharp
using System;

namespace IotModel
{
    public sealed partial class DeviceMqttCredentialDAO : DbContext<DeviceMqttCredential>
    {
        private static DeviceMqttCredentialDAO instance;
        public static DeviceMqttCredentialDAO Instance
        {
            get { return instance ??= new DeviceMqttCredentialDAO(); }
        }

        public override void Init()
        {
            // 凭据由设备签发流程写入,无初始种子。首次访问触发建表即可。
        }
    }
}
```

- [ ] **Step 3: 写 PBKDF2 失败测试**

`IotTests/MqttSecurityTests.cs`:

```csharp
using Xunit;
using IotWebApi;

namespace IotTests
{
    public class MqttSecurityTests
    {
        [Fact]
        public void Pbkdf2_roundtrip_verifies()
        {
            var hash = EncryptsHelper.Pbkdf2Hash("s3cret!", out var salt);
            Assert.True(EncryptsHelper.Pbkdf2Verify("s3cret!", salt, hash));
            Assert.False(EncryptsHelper.Pbkdf2Verify("wrong", salt, hash));
        }
    }
}
```

- [ ] **Step 4: 跑测试确认失败**

Run: `dotnet test IotTests/IotTests.csproj --filter MqttSecurityTests`
Expected: FAIL(`Pbkdf2Hash` 不存在)。

- [ ] **Step 5: 在 EncryptsHelper 加 PBKDF2 静态方法**

`IotWebApi/Common/EncryptsHelper.cs`(类内加,顶部确认 `using System.Security.Cryptography;` `using System.Text;`):

```csharp
/// <summary>PBKDF2(SHA256,100k 迭代)口令哈希。返回 hash 的 base64;salt 经 out 参数以 base64 返回。</summary>
public static string Pbkdf2Hash(string password, out string saltBase64)
{
    byte[] salt = RandomNumberGenerator.GetBytes(16);
    byte[] hash = Rfc2898DeriveBytes.Pbkdf2(Encoding.UTF8.GetBytes(password), salt, 100_000, HashAlgorithmName.SHA256, 32);
    saltBase64 = Convert.ToBase64String(salt);
    return Convert.ToBase64String(hash);
}

/// <summary>定长时间比较校验口令。</summary>
public static bool Pbkdf2Verify(string password, string saltBase64, string hashBase64)
{
    if (string.IsNullOrEmpty(saltBase64) || string.IsNullOrEmpty(hashBase64)) return false;
    byte[] salt = Convert.FromBase64String(saltBase64);
    byte[] expected = Convert.FromBase64String(hashBase64);
    byte[] actual = Rfc2898DeriveBytes.Pbkdf2(Encoding.UTF8.GetBytes(password), salt, 100_000, HashAlgorithmName.SHA256, expected.Length);
    return CryptographicOperations.FixedTimeEquals(actual, expected);
}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `dotnet test IotTests/IotTests.csproj --filter MqttSecurityTests`
Expected: passed。

- [ ] **Step 7: 提交**

```bash
git add IotModel/MEntity/Basic/DeviceMqttCredential.cs IotModel/MDAO/Basic/DeviceMqttCredentialDAO.cs IotWebApi/Common/EncryptsHelper.cs IotTests/MqttSecurityTests.cs
git commit -m "feat(mqtt-sec): device_mqtt_credential 表 + PBKDF2 口令哈希"
```

### Task 4.3: 认证缓存 + ValidatingConnection 改造

**Files:**
- Create: `IotWebApi/Services/Mqtt/MqttCredentialCache.cs`
- Modify: `IotWebApi/Services/Jobs/MqttServerJob.cs`(`ValidatingConnectionAsync`)

**Interfaces:**
- Produces: `MqttCredentialCache.Validate(string mqttUser, string password) : (bool ok, string? deviceGateway)`;`MqttCredentialCache.Invalidate(string mqttUser)`
- Consumes: `DeviceMqttCredentialDAO`、`EncryptsHelper.Pbkdf2Verify`(Task 4.2)

- [ ] **Step 1: 创建两级缓存(照 MqttFlappingGuard 静态 ConcurrentDictionary 风格)**

`IotWebApi/Services/Mqtt/MqttCredentialCache.cs`:

```csharp
using System.Collections.Concurrent;
using IotModel;

namespace IotWebApi.Services.Mqtt
{
    /// <summary>
    /// MQTT 每设备凭据认证的两级缓存:L1 进程内 ConcurrentDictionary(TTL),L2 数据库。
    /// PBKDF2 是慢哈希,连接风暴下靠 L1 命中放行。凭据变更/吊销调 Invalidate 清 L1。
    /// </summary>
    public static class MqttCredentialCache
    {
        private sealed class Cached { public string PassHash = ""; public string Salt = ""; public string? DeviceGateway; public DateTime Expire; }
        private static readonly ConcurrentDictionary<string, Cached> L1 = new();
        private static readonly TimeSpan Ttl = TimeSpan.FromMinutes(10);
        private const int Limit = 10_000;

        public static (bool ok, string? deviceGateway) Validate(string mqttUser, string password)
        {
            if (string.IsNullOrEmpty(mqttUser)) return (false, null);
            var now = DateTime.Now;
            if (L1.TryGetValue(mqttUser, out var c) && c.Expire > now)
                return (EncryptsHelper.Pbkdf2Verify(password, c.Salt, c.PassHash), c.DeviceGateway);

            // L2:查库
            var cred = DeviceMqttCredentialDAO.Instance.GetOneBy(t => t.MqttUser == mqttUser && t.IsEnable);
            if (cred == null) return (false, null);
            var entry = new Cached { PassHash = cred.PassHash, Salt = cred.Salt, DeviceGateway = cred.DeviceGateway, Expire = now.Add(Ttl) };
            PruneIfOversize();
            L1[mqttUser] = entry;
            return (EncryptsHelper.Pbkdf2Verify(password, entry.Salt, entry.PassHash), entry.DeviceGateway);
        }

        public static void Invalidate(string mqttUser) => L1.TryRemove(mqttUser, out _);

        private static void PruneIfOversize()
        {
            if (L1.Count < Limit) return;
            var now = DateTime.Now;
            foreach (var kv in L1) if (kv.Value.Expire <= now) L1.TryRemove(kv.Key, out _);
        }
    }
}
```
> 注:方案约定不引 Redis(单实例 L1+L2 足够,§3.4)。如需 L2 走 Redis,`DbContext.RedisHelper.RedisService` 现成——列为多实例部署时的增量。

- [ ] **Step 2: 改造 ValidatingConnectionAsync(L184 明文比对 → 缓存校验)**

保留 `MqttFlappingGuard` 前置封禁与失败计数,只替换账号比对那一段(L183-190):

```csharp
arg.ReasonCode = MqttConnectReasonCode.Success;

// 内网存量:仍允许全局账号(1883 明文口不断存量);否则走每设备凭据
bool isGlobal = (arg.UserName ?? "") == MqttParam.MqttUser && (arg.Password ?? "") == MqttParam.MqttPass;
if (!isGlobal)
{
    var (ok, gateway) = Services.Mqtt.MqttCredentialCache.Validate(arg.UserName ?? "", arg.Password ?? "");
    if (!ok)
    {
        arg.ReasonCode = MqttConnectReasonCode.BadUserNameOrPassword;
        Mqtt.MqttFlappingGuard.OnAuthFailed(arg.ClientId);
        LogHelper.SysLogWrite("MqttServerJob", "ValidatingConnection", $"凭据校验失败 user={arg.UserName} client={arg.ClientId}", "MqttServer");
        return Task.CompletedTask;
    }
    // 认证通过:把 ClientId→deviceGateway 写入会话映射,供 Topic ACL 用(Task 4.5)
    Mqtt.MqttAclMap.Bind(arg.ClientId, gateway);
}
```
> `MqttAclMap` 在 Task 4.5 创建。此处先写调用,Task 4.5 补类;若要分开编译,Task 4.5 的 `MqttAclMap` 需先于本步的 build 存在——**执行顺序:先做 Task 4.5 的 Step 1(建 MqttAclMap 空类),再回填本步**。或将两 Task 合并提交。

- [ ] **Step 3: 编译 + 提交**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo` → `0 个错误`
```bash
git add IotWebApi/Services/Mqtt/MqttCredentialCache.cs IotWebApi/Services/Jobs/MqttServerJob.cs
git commit -m "feat(mqtt-sec): 每设备凭据两级缓存 + 连接认证改造（存量全局账号并存）"
```

### Task 4.4: Topic ACL

**Files:**
- Create: `IotWebApi/Services/Mqtt/MqttAclMap.cs`
- Modify: `IotWebApi/Services/Jobs/MqttServerJob.cs`(`InterceptingPublishAsync` + 新挂 `InterceptingSubscriptionAsync` + `ClientDisconnected` 清映射)

**Interfaces:**
- Produces: `MqttAclMap.Bind(string clientId, string? deviceGateway)`;`MqttAclMap.Unbind(string clientId)`;`MqttAclMap.Match(string clientId, string topic) : bool`

- [ ] **Step 1: 创建 ACL 会话映射**

`IotWebApi/Services/Mqtt/MqttAclMap.cs`:

```csharp
using System.Collections.Concurrent;

namespace IotWebApi.Services.Mqtt
{
    /// <summary>
    /// ClientId → 绑定的 deviceGateway(deviceKey)。连接认证通过时 Bind,断开时 Unbind。
    /// Topic ACL:校验 topic 末段 == 该连接绑定的 deviceGateway,即使凭据泄露也只能冒充自己那一台。
    /// 全局账号(gateway=null)不参与 ACL(存量内网设备维持现状)。
    /// </summary>
    public static class MqttAclMap
    {
        private static readonly ConcurrentDictionary<string, string> Bindings = new();

        public static void Bind(string clientId, string? deviceGateway)
        {
            if (!string.IsNullOrEmpty(clientId) && !string.IsNullOrEmpty(deviceGateway))
                Bindings[clientId] = deviceGateway!;
        }

        public static void Unbind(string clientId) => Bindings.TryRemove(clientId, out _);

        /// <summary>true=放行。未绑定(全局账号)一律放行;绑定则要求 topic 末段==deviceGateway。</summary>
        public static bool Match(string clientId, string topic)
        {
            if (!Bindings.TryGetValue(clientId, out var gateway)) return true;   // 全局账号,不管 ACL
            var segments = topic.Split('/', System.StringSplitOptions.RemoveEmptyEntries);
            string last = segments.Length > 0 ? segments[^1] : "";
            return last == gateway;
        }
    }
}
```

- [ ] **Step 2: InterceptingPublishAsync 加 ACL(保留原日志)**

在 `InterceptingPublishAsync`(L199-206)日志之后加:

```csharp
if (!Services.Mqtt.MqttAclMap.Match(arg.ClientId, arg.ApplicationMessage.Topic))
{
    arg.ProcessPublish = false;   // ACL 拒绝:该连接无权发到此 topic
    LogHelper.SysLogWrite("MqttServerJob", "InterceptingPublish", $"ACL 拒绝发布 client={arg.ClientId} topic={arg.ApplicationMessage.Topic}", "MqttServer");
}
```

- [ ] **Step 3: 新挂订阅拦截与断开清理(在 broker 事件订阅区 L121-131)**

```csharp
MqttFwdService._mqttServer.InterceptingSubscriptionAsync += arg =>
{
    if (!Services.Mqtt.MqttAclMap.Match(arg.ClientId, arg.TopicFilter.Topic))
        arg.ProcessSubscription = false;
    return Task.CompletedTask;
};
MqttFwdService._mqttServer.ClientDisconnectedAsync += arg =>
{
    Services.Mqtt.MqttAclMap.Unbind(arg.ClientId);
    return Task.CompletedTask;
};
```
> 注:若已有 `ClientDisconnectedAsync` 订阅,把 `Unbind` 合并进现有委托,不要重复挂。

- [ ] **Step 4: 写 ACL 单测**

追加到 `IotTests/MqttSecurityTests.cs`:

```csharp
[Fact]
public void Acl_blocks_cross_device_topic()
{
    IotWebApi.Services.Mqtt.MqttAclMap.Bind("clientA", "devA");
    Assert.True(IotWebApi.Services.Mqtt.MqttAclMap.Match("clientA", "zhjngk/receive/webapi/devA"));   // 自己的 topic
    Assert.False(IotWebApi.Services.Mqtt.MqttAclMap.Match("clientA", "zhjngk/receive/webapi/devB"));  // 冒充他人被拒
    Assert.True(IotWebApi.Services.Mqtt.MqttAclMap.Match("clientGlobal", "any/topic"));               // 未绑定(全局)放行
    IotWebApi.Services.Mqtt.MqttAclMap.Unbind("clientA");
}
```

- [ ] **Step 5: 跑测试 + 编译**

Run: `dotnet test IotTests/IotTests.csproj --filter MqttSecurityTests` → passed
Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo` → `0 个错误`

- [ ] **Step 6: 提交**

```bash
git add IotWebApi/Services/Mqtt/MqttAclMap.cs IotWebApi/Services/Jobs/MqttServerJob.cs IotTests/MqttSecurityTests.cs
git commit -m "feat(mqtt-sec): Topic ACL（ClientId↔deviceKey 绑定，爆炸半径锁单台）"
```

### Task 4.5: 8883 单向 TLS 自签端点

**Files:**
- Modify: `IotWebApi/Services/Jobs/MqttServerJob.cs`

**Interfaces:**
- Consumes: 自签 `X509Certificate2`(运行时生成或从配置路径加载)

- [ ] **Step 1: 加自签证书获取方法(MqttServerJob 内)**

```csharp
private static X509Certificate2 GetOrCreateSelfSignedCert()
{
    // 优先加载配置路径下的 pfx;无则运行时自签一张(仅加密,客户端跳过链校验,不建CA)
    var pfxPath = AppSetting.GetConfig("Mqtt:TlsPfxPath");
    var pfxPass = AppSetting.GetConfig("Mqtt:TlsPfxPass") ?? "";
    if (!string.IsNullOrWhiteSpace(pfxPath) && File.Exists(pfxPath))
        return new X509Certificate2(pfxPath, pfxPass, X509KeyStorageFlags.Exportable);

    using var rsa = RSA.Create(2048);
    var req = new CertificateRequest("CN=zxx-iot-mqtt", rsa, HashAlgorithmName.SHA256, RSASignaturePadding.Pkcs1);
    var cert = req.CreateSelfSigned(DateTimeOffset.Now.AddDays(-1), DateTimeOffset.Now.AddYears(10));
    // 导出再导入,确保私钥可用于服务端 TLS
    return new X509Certificate2(cert.Export(X509ContentType.Pfx), (string?)null, X509KeyStorageFlags.Exportable);
}
```
> 顶部确认 `using System.Security.Cryptography;` `using System.Security.Cryptography.X509Certificates;` `using System.IO;`。

- [ ] **Step 2: 追加 8883 加密端点(选项构建区 L108-114)**

```csharp
// 现有默认端点(1883,已在 Task 4.1 收窄)之后追加:
optionsBuilder.WithEncryptedEndpoint();
optionsBuilder.WithEncryptedEndpointPort(8883);
optionsBuilder.WithEncryptionCertificate(GetOrCreateSelfSignedCert());
optionsBuilder.WithEncryptionSslProtocol(System.Security.Authentication.SslProtocols.Tls12 | System.Security.Authentication.SslProtocols.Tls13);
```
> ⚠ 部署前提(非代码):8883 需真实公网可达(cloudflared 不适用,需端口映射);1883 内网口不得映射公网。落地前不暴露任何 MQTT 端口到外网。

- [ ] **Step 3: 编译 + 提交**

Run: `dotnet build IotWebApi/IotWebApi.csproj -c Debug --nologo` → `0 个错误`
```bash
git add IotWebApi/Services/Jobs/MqttServerJob.cs
git commit -m "feat(mqtt-sec): 8883 单向 TLS 自签端点（仅加密，客户端跳过链校验）"
```

### Task 4.6: 集成验证(人工)

- [ ] **Step 1: 验证清单(参考 `docs/外网MQTT安全接入方案.md` §7)**

需启用内嵌 broker(`JobInitializer.cs:25` 把 MqttServerJob 的 `JobStatus` 改 1,或手动触发),然后:
1. 正确凭据连 8883(TLS,客户端跳过链校验)→ 成功,抓包看不到明文。
2. 错误凭据连 8883 → 拒绝,`MqttFlappingGuard` 计数。
3. 设备 A 的凭据往设备 B 的 topic publish → **被 ACL 拒绝**(核心断言)。
4. 1883 明文口 + 全局账号存量链路 → 行为不变。

现有 MQTT 上行端到端(Phase 1 那条链路)必须仍全绿。

---

## Self-Review(撰写者自检)

**1. Spec 覆盖**:
- 验证重连 → Phase 1(人工清单,诚实标注环境依赖)✅
- 北向设备状态(方案三坑:字典依赖/屏蔽吞/首次上线无事件)→ Phase 2,新出口不进告警生命周期即解三坑 ✅
- 南向 TCP/UDP(UplinkPayloadRouter 提取 / IP 认设备 / splitFrames)→ Phase 3;splitFrames 明确降为 Task 3.5 增量项并说明原因 ✅
- 外网 MQTT(内网收窄/凭据/缓存/ACL/TLS,X.509 排除)→ Phase 4 五个可独立发布的 Task ✅

**2. Placeholder 扫描**:无 "TBD/TODO/略";每个代码步骤给完整代码。splitFrames 脚本定界标注为增量而非 placeholder(第一版 `\n` 定界是完整可跑的)。

**3. 类型一致性**:
- `ForwardDeviceState(int,string,int,string)` 在 Task 2.1 定义、2.2 消费,签名一致 ✅
- `UplinkPayloadRouter.Route(string,byte[])` 在 3.2 定义,3.4/3.5 消费,一致 ✅
- `EncryptsHelper.Pbkdf2Hash/Verify`、`MqttCredentialCache.Validate`、`MqttAclMap.Bind/Unbind/Match` 定义与消费签名一致 ✅
- ⚠ **执行顺序依赖**:Task 4.3 Step 2 引用 `MqttAclMap.Bind`(Task 4.4 才创建)。**实施时先建 Task 4.4 的 `MqttAclMap` 类,再回填 4.3 的绑定调用**,或把 4.3/4.4 合并为一次提交。已在 4.3 Step 2 注明。

**4. 已知残留(非本计划范围,方案文档已记)**:
- `IChannelTransport` 只声明 `Send`,收帧五件套无接口约束(技术债,不在此重构)。
- UDP 无可靠性重传(YAGNI,需可靠用 TCP/MQTT)。
- splitFrames 脚本定界(Task 3.5 增量)。
- L2 Redis 认证缓存(多实例时再补)。
