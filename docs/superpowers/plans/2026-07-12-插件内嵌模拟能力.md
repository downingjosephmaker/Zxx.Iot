# 插件内嵌模拟能力 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 撤销独立的 `IotSimulator`/`IotSimulator.Core` 项目，把设备模拟能力内嵌进各协议插件，通过统一 `ISimulatable` 接口 + `SimulatorController` REST 层触发，UI 落在设备管理页（设备行「采集」「模拟」双按钮 + 模拟弹窗）。

**Architecture:** 每个协议插件在实现 `ICenBoPlugin`（采集主站）之外，可选实现 `ISimulatable`（模拟从站，独立端口/生命周期）。协议无关的值生成器与故障注入下沉 `IotDriverCore/Simulation/` 共享；各协议从站编解码整体迁入对应插件 `Sim/` 目录，保持独立实现（对抗性验证）；数据源复用平台 `DeviceInfo`+`DeviceTypeParam` 点表；帧日志经现成 SignalR 回流前端。

**Tech Stack:** .NET 10、SqlSugar ORM、FluentModbus（Modbus 从站）、NewLife.Core、SignalR（ChatServer）、Vue3 + Element Plus（pure-admin 血脉）、AssemblyLoadContext 插件热加载。

## Global Constraints

- **目标框架**：所有 .NET 项目 `net10.0`，`Nullable=enable`、`ImplicitUsings=enable`。
- **编解码独立**：从站模拟的编解码**不得调用插件 FrameHelper**，保留原作者对抗性验证设计意图。
- **模拟独立端口**：模拟从站用独立监听端口，不与采集通道（TcpServerChannel）纠缠（方案 A）。
- **数据源复用平台点表**：模拟设备数据来自 `DeviceInfo`+`DeviceTypeParam`，生成器覆盖参数不落库。
- **日志风格**：后端日志用 `LogHelper.SysLogWrite`/`LogHelper.ErrorLogWrite`（非 `LogHelper.Info/Error`），签名 `(className, methodName, message, category)`。
- **ISimulatable 可选**：非所有插件必须实现；OpcUa/S7 本期不强制实现模拟从站。
- **提交规范**：Conventional Commit，中文描述，无 AI 署名。每任务独立提交。
- **验证命令**：后端 `dotnet build IotWebApi.sln`；测试 `dotnet test IotTests/IotTests.csproj`；前端 `pnpm -C vuefrontend build`。

---

## 文件结构（改动地图）

### 新建
- `IotDriverCore/Contracts/ISimulatable.cs` — 模拟人格契约
- `IotDriverCore/Contracts/SimModels.cs` — SimCapability/SimStartRequest/SimStatus/SimFaultSpec/SimLogEntry/SimPoint/SimDevice
- `IotDriverCore/Simulation/IValueGenerator.cs` — 值生成器（从 IotSimulator.Core 下沉）
- `IotDriverCore/Simulation/SimGeneratorModels.cs` — GeneratorModel/FaultModel（从 ScenarioModel.cs 裁剪下沉）
- `IotDriverCore/Simulation/FaultInjector.cs` — 故障注入（下沉）
- `IotPlugin.Modbus/Sim/ModbusSlave.cs` — Modbus 从站（迁入 + 改吃 SimDevice）
- `IotPlugin.Modbus/Sim/ModbusSimulator.cs` — Modbus 插件的 ISimulatable 实现
- `IotPlugin.Dlt645/Sim/Dlt645Slave.cs`、`IProtocolSlave.cs`、`Dlt645Simulator.cs`
- `IotPlugin.Cjt188/Sim/Cjt188Slave.cs`、`Cjt188Simulator.cs`
- `IotWebApi/Areas/Device/Controllers/SimulatorController.cs` — REST 层
- `vuefrontend/src/views/iot/device/sim-dialog.vue` — 模拟弹窗（两标签页）
- `vuefrontend/src/api/simulator.ts` — 前端 API 封装

### 修改
- `IotPlugin.Modbus/IotPlugin.Modbus.csproj` — 加 FluentModbus 包引用
- `IotPlugin.Modbus/ModbusPlugin.cs`、`IotPlugin.Dlt645/Dlt645Plugin.cs`、`IotPlugin.Cjt188/Cjt188Plugin.cs` — 类声明加 `, ISimulatable`，委托给 Simulator
- `IotWebApi/Areas/Device/Controllers/DeviceInfoController.cs` — 加 `ToggleCollection` 端点
- `vuefrontend/src/views/iot/device/utils/hook.tsx` — 操作列加双按钮
- `vuefrontend/src/views/iot/device/index.vue` — 挂载模拟弹窗
- `IotTests/IotTests.csproj` — 移除 IotSimulator.Core 引用
- `IotTests/FaultInjectorTests.cs`、`SlaveRoundTripTests.cs` — 改 using 指向新位置
- `IotWebApi.sln` — 移除两项目

### 删除
- `IotSimulator/`（整个）
- `IotSimulator.Core/Scenario/`、`Transport/`、`Slaves/ModbusSlave.cs`（迁走后删原件）、整个 `IotSimulator.Core/` 收尾

---

## Task 1: 下沉值生成器与故障模型到 IotDriverCore

**Files:**
- Create: `IotDriverCore/Simulation/SimGeneratorModels.cs`
- Create: `IotDriverCore/Simulation/IValueGenerator.cs`
- Create: `IotDriverCore/Simulation/FaultInjector.cs`
- Test: `IotTests/SimulationCoreTests.cs`

**Interfaces:**
- Produces:
  - `namespace IotDriverCore.Simulation`
  - `class GeneratorModel { string Type; double Base,Min,Max,Amp,PeriodS,Step,StepEveryS; }`
  - `class FaultModel { string Type; double Probability; int DelayMs; }`
  - `interface IValueGenerator { double Next(DateTime now); }`
  - `static class GeneratorFactory { IValueGenerator Create(GeneratorModel model); }`
  - `class FaultDecision { bool Drop; List<byte[]> Segments; int DelayMs; static Whole(byte[]); static Dropped(); }`
  - `class FaultInjector { FaultInjector(List<FaultModel>?, Func<byte[],byte[]>?); FaultDecision Decorate(byte[]); }`

- [ ] **Step 1: 写 SimGeneratorModels.cs（GeneratorModel/FaultModel 裁剪自 ScenarioModel.cs）**

从 `IotSimulator.Core/Scenario/ScenarioModel.cs` 第 100-140 行原样复制 `GeneratorModel`、`FaultModel` 两个类到新文件，命名空间改 `IotDriverCore.Simulation`：

```csharp
namespace IotDriverCore.Simulation
{
    /// <summary>值生成器模型(type=constant/random/sine/step;从模拟器场景模型下沉,协议无关)</summary>
    public class GeneratorModel
    {
        public string Type { get; set; } = "constant";
        public double Base { get; set; }
        public double Min { get; set; }
        public double Max { get; set; }
        public double Amp { get; set; }
        public double PeriodS { get; set; } = 60;
        public double Step { get; set; } = 1;
        public double StepEveryS { get; set; } = 10;
    }

    /// <summary>故障注入模型(type=timeout/wrongcs/stick/split)</summary>
    public class FaultModel
    {
        public string Type { get; set; } = "";
        public double Probability { get; set; } = 1;
        public int DelayMs { get; set; } = 50;
    }
}
```

- [ ] **Step 2: 写 IValueGenerator.cs（整体搬 IotSimulator.Core/Generators/IValueGenerator.cs）**

复制 `IotSimulator.Core/Generators/IValueGenerator.cs` 全文（含 `IValueGenerator`/`GeneratorFactory`/`ConstantGenerator`/`RandomGenerator`/`SineGenerator`/`StepGenerator`），改两处：
- 命名空间 `IotSimulator.Core.Generators` → `IotDriverCore.Simulation`
- 删除 `using IotSimulator.Core.Scenario;`（GeneratorModel 已同命名空间）

- [ ] **Step 3: 写 FaultInjector.cs（整体搬 IotSimulator.Core/Faults/FaultInjector.cs）**

复制 `IotSimulator.Core/Faults/FaultInjector.cs` 全文（含 `FaultDecision`/`FaultInjector`），改两处：
- 命名空间 `IotSimulator.Core.Faults` → `IotDriverCore.Simulation`
- 删除 `using IotSimulator.Core.Scenario;`（FaultModel 已同命名空间）

- [ ] **Step 4: 写失败测试 SimulationCoreTests.cs**

```csharp
using IotDriverCore.Simulation;
using Xunit;

public class SimulationCoreTests
{
    [Fact]
    public void ConstantGenerator_ReturnsBase()
    {
        var g = GeneratorFactory.Create(new GeneratorModel { Type = "constant", Base = 42 });
        Assert.Equal(42, g.Next(DateTime.Now));
    }

    [Fact]
    public void FaultInjector_Timeout_Drops()
    {
        var inj = new FaultInjector(new List<FaultModel> { new() { Type = "timeout", Probability = 1 } });
        var decision = inj.Decorate(new byte[] { 1, 2, 3 });
        Assert.True(decision.Drop);
    }

    [Fact]
    public void FaultInjector_NoFault_SendsWhole()
    {
        var inj = new FaultInjector(null);
        var decision = inj.Decorate(new byte[] { 1, 2, 3 });
        Assert.False(decision.Drop);
        Assert.Single(decision.Segments);
    }
}
```

- [ ] **Step 5: 运行测试确认失败（尚未加 IotTests→无引用会编译错，或断言未通过）**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~SimulationCoreTests`
Expected: FAIL（编译错误：`IotDriverCore.Simulation` 未被 IotTests 引用；IotTests 已引用 IotDriverCore，实际应通过——若失败则因命名空间未生效）

- [ ] **Step 6: 运行测试确认通过**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~SimulationCoreTests`
Expected: PASS（3 passed）

- [ ] **Step 7: 提交**

```bash
git add IotDriverCore/Simulation/ IotTests/SimulationCoreTests.cs
git commit -m "feat(sim): 值生成器与故障注入下沉IotDriverCore.Simulation"
```

---

## Task 2: 定义 ISimulatable 契约与数据模型

**Files:**
- Create: `IotDriverCore/Contracts/SimModels.cs`
- Create: `IotDriverCore/Contracts/ISimulatable.cs`
- Test: `IotTests/SimContractTests.cs`

**Interfaces:**
- Consumes: `IotDriverCore.Simulation.GeneratorModel`、`FaultModel`（Task 1）
- Produces:
  - `namespace IotDriverCore`
  - `class SimPoint { string ParamCode; string Di; int FuncCode; int Length; string DataType; GeneratorModel Generator; }`
  - `class SimDevice { string Address; string? MeterType; List<SimPoint> Points; List<FaultModel> Faults; }`
  - `class SimCapability { bool SupportSlave; bool SupportSelfTest; int DefaultPort; string Protocol; }`
  - `enum SimMode { Slave, SelfTest }`
  - `class SimStartRequest { SimMode Mode; int Port; List<SimDevice> Devices; }`
  - `class SimStatus { string SimId; int DeviceId; SimMode Mode; bool Running; int Port; string StartedAt; string Message; }`
  - `class SimFaultSpec { string Kind; double Probability; int DelayMs; }`
  - `class SimLogEntry { string SimId; string Time; string Direction; string Hex; string Note; }`
  - `interface ISimulatable { SimCapability Capability {get;} Task<SimStatus> StartSimAsync(SimStartRequest, CancellationToken); Task StopSimAsync(string simId); IReadOnlyList<SimStatus> ListSims(); Task InjectFaultAsync(string simId, SimFaultSpec); Action<SimLogEntry>? OnSimLog {get;set;} }`

- [ ] **Step 1: 写 SimModels.cs**

```csharp
using IotDriverCore.Simulation;

namespace IotDriverCore
{
    /// <summary>模拟点位(数据源为平台DeviceTypeParam点表快照)</summary>
    public class SimPoint
    {
        /// <summary>参数编码(平台ParamCode)</summary>
        public string ParamCode { get; set; } = "";
        /// <summary>标识(645/188为DI;modbus为寄存器地址,十进制或0x前缀)</summary>
        public string Di { get; set; } = "";
        /// <summary>modbus功能码(3保持/4输入;645/188忽略)</summary>
        public int FuncCode { get; set; } = 3;
        /// <summary>值字节数/寄存器数</summary>
        public int Length { get; set; } = 4;
        /// <summary>数据类型(uint16/int16/int32/float32等)</summary>
        public string DataType { get; set; } = "uint16";
        /// <summary>值生成器(默认随机游走,UI可覆盖,不落库)</summary>
        public GeneratorModel Generator { get; set; } = new();
    }

    /// <summary>模拟设备(一个从站)</summary>
    public class SimDevice
    {
        /// <summary>设备地址(645/188表地址;modbus从站号)</summary>
        public string Address { get; set; } = "";
        /// <summary>表型(仅cjt188)</summary>
        public string? MeterType { get; set; }
        /// <summary>点位清单</summary>
        public List<SimPoint> Points { get; set; } = new();
        /// <summary>设备级故障注入</summary>
        public List<FaultModel> Faults { get; set; } = new();
    }

    /// <summary>模拟能力元信息(前端渲染用)</summary>
    public class SimCapability
    {
        public bool SupportSlave { get; set; }
        public bool SupportSelfTest { get; set; }
        public int DefaultPort { get; set; }
        public string Protocol { get; set; } = "";
    }

    /// <summary>模拟模式</summary>
    public enum SimMode { Slave, SelfTest }

    /// <summary>启动模拟请求</summary>
    public class SimStartRequest
    {
        public SimMode Mode { get; set; } = SimMode.Slave;
        public int Port { get; set; }
        public List<SimDevice> Devices { get; set; } = new();
    }

    /// <summary>模拟实例状态</summary>
    public class SimStatus
    {
        public string SimId { get; set; } = "";
        public int DeviceId { get; set; }
        public SimMode Mode { get; set; }
        public bool Running { get; set; }
        public int Port { get; set; }
        public string StartedAt { get; set; } = "";
        public string Message { get; set; } = "";
    }

    /// <summary>故障注入规格(UI→运行中实例)</summary>
    public class SimFaultSpec
    {
        /// <summary>类型:timeout/wrongcs/stick/split;空串=清除</summary>
        public string Kind { get; set; } = "";
        public double Probability { get; set; } = 1;
        public int DelayMs { get; set; } = 50;
    }

    /// <summary>实时帧日志(经SignalR回流)</summary>
    public class SimLogEntry
    {
        public string SimId { get; set; } = "";
        public string Time { get; set; } = "";
        /// <summary>方向:← 收 / → 发</summary>
        public string Direction { get; set; } = "";
        public string Hex { get; set; } = "";
        public string Note { get; set; } = "";
    }
}
```

- [ ] **Step 2: 写 ISimulatable.cs**

```csharp
namespace IotDriverCore
{
    /// <summary>
    /// 协议模拟人格契约(插件可选实现;与采集能力共存于同一插件实例,
    /// 但独立端口/独立生命周期,不与采集通道纠缠——方案A隔离原则)
    /// </summary>
    public interface ISimulatable
    {
        /// <summary>模拟能力元信息(支持的模式/默认端口等,前端渲染用)</summary>
        SimCapability Capability { get; }

        /// <summary>启动模拟(传入选中设备+点表快照+运行参数,返回运行句柄状态)</summary>
        Task<SimStatus> StartSimAsync(SimStartRequest request, CancellationToken ct);

        /// <summary>停止指定模拟实例</summary>
        Task StopSimAsync(string simId);

        /// <summary>查询当前所有模拟实例状态</summary>
        IReadOnlyList<SimStatus> ListSims();

        /// <summary>注入/清除故障(错帧/超时/半包等)</summary>
        Task InjectFaultAsync(string simId, SimFaultSpec fault);

        /// <summary>实时日志回调(收发帧hex摘要,宿主接到后转推SignalR)</summary>
        Action<SimLogEntry>? OnSimLog { get; set; }
    }
}
```

- [ ] **Step 3: 写失败测试 SimContractTests.cs**

```csharp
using IotDriverCore;
using IotDriverCore.Simulation;
using Xunit;

public class SimContractTests
{
    [Fact]
    public void SimStartRequest_DefaultsToSlaveMode()
    {
        var req = new SimStartRequest();
        Assert.Equal(SimMode.Slave, req.Mode);
    }

    [Fact]
    public void SimPoint_DefaultGenerator_NotNull()
    {
        var p = new SimPoint();
        Assert.NotNull(p.Generator);
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~SimContractTests`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
git add IotDriverCore/Contracts/SimModels.cs IotDriverCore/Contracts/ISimulatable.cs IotTests/SimContractTests.cs
git commit -m "feat(sim): 定义ISimulatable契约与Sim数据模型"
```

---

## Task 3: 迁移 Modbus 从站进插件并实现 ISimulatable

**Files:**
- Modify: `IotPlugin.Modbus/IotPlugin.Modbus.csproj`（加 FluentModbus 包）
- Create: `IotPlugin.Modbus/Sim/ModbusSlave.cs`（迁入 + 改吃 SimDevice）
- Create: `IotPlugin.Modbus/Sim/ModbusSimulator.cs`（ISimulatable 实现）
- Modify: `IotPlugin.Modbus/ModbusPlugin.cs`（类声明加 ISimulatable，委托）
- Test: `IotTests/ModbusSimTests.cs`

**Interfaces:**
- Consumes: `IotDriverCore.ISimulatable`、`SimDevice`、`SimStartRequest`、`SimStatus`、`IotDriverCore.Simulation.GeneratorFactory`（Task 1/2）
- Produces:
  - `namespace IotPlugin.Modbus.Sim`
  - `class ModbusSlave { ModbusSlave(int port, IEnumerable<SimDevice> devices); void Start(); void Stop(); Action<string>? OnLog; }`
  - `class ModbusSimulator : ISimulatable`（供 ModbusPlugin 内部委托）

- [ ] **Step 1: csproj 加 FluentModbus 包引用**

在 `IotPlugin.Modbus/IotPlugin.Modbus.csproj` 的第一个 `<ItemGroup>`（ProjectReference 那组）后新增：

```xml
  <ItemGroup>
    <PackageReference Include="FluentModbus" Version="5.0.2" />
  </ItemGroup>
```

（版本对齐 IotSimulator.Core 现用版本；若不同以 `IotSimulator.Core/IotSimulator.Core.csproj` 里的 FluentModbus 版本为准。）

- [ ] **Step 2: 迁 ModbusSlave.cs（改数据源为 SimDevice）**

复制 `IotSimulator.Core/Slaves/ModbusSlave.cs` 全文到 `IotPlugin.Modbus/Sim/ModbusSlave.cs`，改动：
- 命名空间 → `IotPlugin.Modbus.Sim`
- using：`IotSimulator.Core.Generators` → `IotDriverCore.Simulation`；`IotSimulator.Core.Scenario` → `IotDriverCore`
- 构造签名 `IEnumerable<DeviceModel> devices` → `IEnumerable<SimDevice> devices`
- 遍历点位 `pm.FuncCode/pm.Di/pm.DataType/pm.Length/pm.Generator` 全部适配 SimPoint 字段（字段名一致：`FuncCode`/`Di`/`DataType`/`Length`/`Generator`，无需改）
- 设备 `device.Address` → `device.Address`（一致）

- [ ] **Step 3: 写 ModbusSimulator.cs**

```csharp
using IotDriverCore;
using IotPlugin.Modbus.Sim;

namespace IotPlugin.Modbus.Sim
{
    /// <summary>Modbus插件的模拟人格(独立端口起FluentModbus从站,平台采集器可连上读假数据)</summary>
    public sealed class ModbusSimulator : ISimulatable
    {
        private readonly object _lock = new();
        private readonly Dictionary<string, (ModbusSlave slave, SimStatus status)> _sims = new();

        public SimCapability Capability => new()
        {
            SupportSlave = true,
            SupportSelfTest = false,
            DefaultPort = 502,
            Protocol = "modbus"
        };

        public Action<SimLogEntry>? OnSimLog { get; set; }

        public Task<SimStatus> StartSimAsync(SimStartRequest request, CancellationToken ct)
        {
            string simId = $"modbus-{request.Port}-{Guid.NewGuid():N}".Substring(0, 20);
            var slave = new ModbusSlave(request.Port, request.Devices)
            {
                OnLog = msg => OnSimLog?.Invoke(new SimLogEntry
                {
                    SimId = simId, Time = DateTime.Now.ToString("HH:mm:ss.fff"),
                    Direction = "", Hex = "", Note = msg
                })
            };
            var status = new SimStatus
            {
                SimId = simId,
                DeviceId = 0,
                Mode = SimMode.Slave,
                Running = true,
                Port = request.Port,
                StartedAt = DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss"),
                Message = "已启动"
            };
            slave.Start();
            lock (_lock) { _sims[simId] = (slave, status); }
            return Task.FromResult(status);
        }

        public Task StopSimAsync(string simId)
        {
            lock (_lock)
            {
                if (_sims.Remove(simId, out var entry)) entry.slave.Stop();
            }
            return Task.CompletedTask;
        }

        public IReadOnlyList<SimStatus> ListSims()
        {
            lock (_lock) { return _sims.Values.Select(v => v.status).ToList(); }
        }

        public Task InjectFaultAsync(string simId, SimFaultSpec fault)
        {
            // Modbus从站为FluentModbus托管寄存器区,故障注入本期不适用(仅串行协议支持),空实现
            return Task.CompletedTask;
        }
    }
}
```

- [ ] **Step 4: ModbusPlugin.cs 加 ISimulatable（委托内部 Simulator）**

`ModbusPlugin` 类声明改为 `public class ModbusPlugin : ICenBoPlugin, ISimulatable`，在类内加字段与委托：

```csharp
        // ===== 模拟人格(方案A:独立端口/生命周期,委托ModbusSimulator) =====
        private readonly Sim.ModbusSimulator _simulator = new();

        public SimCapability Capability => _simulator.Capability;
        public Action<SimLogEntry>? OnSimLog
        {
            get => _simulator.OnSimLog;
            set => _simulator.OnSimLog = value;
        }
        public Task<SimStatus> StartSimAsync(SimStartRequest request, CancellationToken ct) => _simulator.StartSimAsync(request, ct);
        public Task StopSimAsync(string simId) => _simulator.StopSimAsync(simId);
        public IReadOnlyList<SimStatus> ListSims() => _simulator.ListSims();
        public Task InjectFaultAsync(string simId, SimFaultSpec fault) => _simulator.InjectFaultAsync(simId, fault);
```

在文件顶部加 `using IotDriverCore;`（若未含）。

- [ ] **Step 5: 写失败测试 ModbusSimTests.cs（从站起停 + 客户端读到值）**

```csharp
using FluentModbus;
using IotDriverCore;
using IotDriverCore.Simulation;
using IotPlugin.Modbus.Sim;
using System.Net;
using Xunit;

public class ModbusSimTests
{
    [Fact]
    public async Task ModbusSimulator_StartStop_Roundtrip()
    {
        var sim = new ModbusSimulator();
        var req = new SimStartRequest
        {
            Mode = SimMode.Slave,
            Port = 15020,
            Devices = new List<SimDevice>
            {
                new()
                {
                    Address = "1",
                    Points = new List<SimPoint>
                    {
                        new() { Di = "0", FuncCode = 3, DataType = "uint16", Length = 1,
                                Generator = new GeneratorModel { Type = "constant", Base = 123 } }
                    }
                }
            }
        };
        var status = await sim.StartSimAsync(req, default);
        Assert.True(status.Running);

        await Task.Delay(1500); // 等一次寄存器刷新

        var client = new ModbusTcpClient();
        client.Connect(new IPEndPoint(IPAddress.Loopback, 15020));
        var regs = client.ReadHoldingRegisters<ushort>(1, 0, 1);
        Assert.Equal(123, regs[0]);
        client.Disconnect();

        await sim.StopSimAsync(status.SimId);
        Assert.Empty(sim.ListSims());
    }
}
```

- [ ] **Step 6: 运行测试确认失败（迁移前后编译/端口）**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~ModbusSimTests`
Expected: FAIL（首次因 IotTests 未引用 IotPlugin.Modbus.Sim 命名空间新类型，或断言前编译错）

- [ ] **Step 7: 运行测试确认通过**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~ModbusSimTests`
Expected: PASS（1 passed，客户端读到 123）

- [ ] **Step 8: 提交**

```bash
git add IotPlugin.Modbus/ IotTests/ModbusSimTests.cs
git commit -m "feat(sim): Modbus从站迁入插件并实现ISimulatable(独立端口FluentModbus从站)"
```

---

## Task 4: 迁移 DL/T645 从站进插件并实现 ISimulatable

**Files:**
- Create: `IotPlugin.Dlt645/Sim/IProtocolSlave.cs`（迁入接口）
- Create: `IotPlugin.Dlt645/Sim/Dlt645Slave.cs`（迁入 + 改吃 SimDevice）
- Create: `IotPlugin.Dlt645/Sim/Dlt645Simulator.cs`（ISimulatable 实现，串行从站 + 故障注入）
- Modify: `IotPlugin.Dlt645/Dlt645Plugin.cs`（类声明加 ISimulatable，委托）
- Test: `IotTests/Dlt645SimTests.cs`

**Interfaces:**
- Consumes: `IotDriverCore.ISimulatable`、`SimDevice`、`IotDriverCore.Simulation.FaultInjector`、`IotDriverCore.Channels.TcpServerChannel`、`FrameAccumulator`（现有）
- Produces:
  - `namespace IotPlugin.Dlt645.Sim`
  - `interface IProtocolSlave { string Address; byte[]? HandleFrame(byte[] frame, DateTime now); byte[] Corrupt(byte[] frame); }`
  - `class Dlt645Slave : IProtocolSlave { Dlt645Slave(SimDevice device, bool is1997); }`
  - `class Dlt645Simulator : ISimulatable`

- [ ] **Step 1: 迁 IProtocolSlave.cs**

复制 `IotSimulator.Core/Slaves/IProtocolSlave.cs` 到 `IotPlugin.Dlt645/Sim/IProtocolSlave.cs`，命名空间 → `IotPlugin.Dlt645.Sim`。

- [ ] **Step 2: 迁 Dlt645Slave.cs（改数据源 DeviceModel→SimDevice）**

复制 `IotSimulator.Core/Slaves/Dlt645Slave.cs` 到 `IotPlugin.Dlt645/Sim/Dlt645Slave.cs`，改动：
- 命名空间 → `IotPlugin.Dlt645.Sim`
- using：`IotSimulator.Core.Generators` → `IotDriverCore.Simulation`；`IotSimulator.Core.Scenario` → `IotDriverCore`
- 构造参数 `DeviceModel device` → `SimDevice device`；内部访问的 `device.Address`/`device.Points`/`point.Di`/`point.Length`/`point.Generator` 字段名一致，无需改
- **编解码逻辑一字不改**（保留对抗性验证）

- [ ] **Step 3: 写 Dlt645Simulator.cs（串行从站宿主：TcpServerChannel + FrameAccumulator + FaultInjector）**

```csharp
using IotDriverCore;
using IotDriverCore.Channels;
using IotDriverCore.Simulation;

namespace IotPlugin.Dlt645.Sim
{
    /// <summary>DL/T645插件的模拟人格(独立端口监听,收主站请求帧→逐从站编应答→故障装饰→回发)</summary>
    public sealed class Dlt645Simulator : ISimulatable
    {
        private sealed class SimInstance
        {
            public TcpServerChannel Channel = null!;
            public FrameAccumulator Accumulator = null!;
            public List<IProtocolSlave> Slaves = new();
            public Dictionary<IProtocolSlave, FaultInjector> Injectors = new();
            public SimStatus Status = null!;
        }

        private readonly object _lock = new();
        private readonly Dictionary<string, SimInstance> _sims = new();

        public SimCapability Capability => new()
        {
            SupportSlave = true, SupportSelfTest = false, DefaultPort = 9645, Protocol = "dlt645"
        };

        public Action<SimLogEntry>? OnSimLog { get; set; }

        public Task<SimStatus> StartSimAsync(SimStartRequest request, CancellationToken ct)
        {
            string simId = $"dlt645-{request.Port}-{Guid.NewGuid():N}"[..22];
            var inst = new SimInstance
            {
                Accumulator = new FrameAccumulator(FrameAccumulator.ExtractDlt645),
                Status = new SimStatus
                {
                    SimId = simId, Mode = SimMode.Slave, Running = true, Port = request.Port,
                    StartedAt = DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss"), Message = "已启动"
                }
            };
            foreach (var dev in request.Devices)
            {
                var slave = new Dlt645Slave(dev, is1997: false);
                inst.Slaves.Add(slave);
                inst.Injectors[slave] = new FaultInjector(dev.Faults, slave.Corrupt);
            }
            inst.Channel = new TcpServerChannel(request.Port)
            {
                FrameReceived = (ep, data) => OnInbound(simId, inst, ep, data)
            };
            inst.Channel.Start();
            lock (_lock) { _sims[simId] = inst; }
            return Task.FromResult(inst.Status);
        }

        private void OnInbound(string simId, SimInstance inst, string endpoint, byte[] data)
        {
            var now = DateTime.Now;
            foreach (var frame in inst.Accumulator.Push(endpoint, data))
            {
                Log(simId, "←", frame, "收到请求");
                foreach (var slave in inst.Slaves)
                {
                    var reply = slave.HandleFrame(frame, now);
                    if (reply == null) continue;
                    var decision = inst.Injectors[slave].Decorate(reply);
                    if (decision.Drop) { Log(simId, "→", Array.Empty<byte>(), $"从站[{slave.Address}]故障丢弃"); break; }
                    foreach (var seg in decision.Segments)
                    {
                        inst.Channel.Send(endpoint, seg);
                        Log(simId, "→", seg, $"从站[{slave.Address}]应答");
                        if (decision.DelayMs > 0) Thread.Sleep(decision.DelayMs);
                    }
                    break;
                }
            }
        }

        private void Log(string simId, string dir, byte[] frame, string note) =>
            OnSimLog?.Invoke(new SimLogEntry
            {
                SimId = simId, Time = DateTime.Now.ToString("HH:mm:ss.fff"),
                Direction = dir, Hex = frame.Length > 0 ? Convert.ToHexString(frame) : "", Note = note
            });

        public Task StopSimAsync(string simId)
        {
            lock (_lock)
            {
                if (_sims.Remove(simId, out var inst)) inst.Channel.Dispose();
            }
            return Task.CompletedTask;
        }

        public IReadOnlyList<SimStatus> ListSims()
        {
            lock (_lock) { return _sims.Values.Select(v => v.Status).ToList(); }
        }

        public Task InjectFaultAsync(string simId, SimFaultSpec fault)
        {
            lock (_lock)
            {
                if (!_sims.TryGetValue(simId, out var inst)) return Task.CompletedTask;
                var model = fault.Kind.Length == 0 ? new List<FaultModel>() :
                    new List<FaultModel> { new() { Type = fault.Kind, Probability = fault.Probability, DelayMs = fault.DelayMs } };
                foreach (var slave in inst.Slaves)
                    inst.Injectors[slave] = new FaultInjector(model, slave.Corrupt);
            }
            return Task.CompletedTask;
        }
    }
}
```

- [ ] **Step 4: Dlt645Plugin.cs 加 ISimulatable 委托**

`Dlt645Plugin` 类声明加 `, ISimulatable`，加与 Task 3 Step 4 同构的委托块（把 `Sim.ModbusSimulator` 换成 `Sim.Dlt645Simulator`），顶部加 `using IotDriverCore;`。

- [ ] **Step 5: 写失败测试 Dlt645SimTests.cs（起停 + 端口监听就绪）**

```csharp
using IotDriverCore;
using IotDriverCore.Simulation;
using IotPlugin.Dlt645.Sim;
using Xunit;

public class Dlt645SimTests
{
    [Fact]
    public async Task Dlt645Simulator_StartStop()
    {
        var sim = new Dlt645Simulator();
        var req = new SimStartRequest
        {
            Mode = SimMode.Slave, Port = 19645,
            Devices = new List<SimDevice>
            {
                new() { Address = "000000000001",
                        Points = new List<SimPoint> { new() { Di = "0x02010100", Length = 4,
                            Generator = new GeneratorModel { Type = "constant", Base = 220 } } } }
            }
        };
        var status = await sim.StartSimAsync(req, default);
        Assert.True(status.Running);
        Assert.Single(sim.ListSims());
        await sim.StopSimAsync(status.SimId);
        Assert.Empty(sim.ListSims());
    }
}
```

- [ ] **Step 6: 运行测试确认通过**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~Dlt645SimTests`
Expected: PASS（1 passed）

- [ ] **Step 7: 提交**

```bash
git add IotPlugin.Dlt645/ IotTests/Dlt645SimTests.cs
git commit -m "feat(sim): DLT645从站迁入插件并实现ISimulatable(串行从站+故障注入)"
```

---

## Task 5: 迁移 CJ/T188 从站进插件并实现 ISimulatable

**Files:**
- Create: `IotPlugin.Cjt188/Sim/IProtocolSlave.cs`、`Cjt188Slave.cs`、`Cjt188Simulator.cs`
- Modify: `IotPlugin.Cjt188/Cjt188Plugin.cs`
- Test: `IotTests/Cjt188SimTests.cs`

**Interfaces:**
- Consumes: 同 Task 4
- Produces: `namespace IotPlugin.Cjt188.Sim`；`class Cjt188Slave : IProtocolSlave { Cjt188Slave(SimDevice device); }`；`class Cjt188Simulator : ISimulatable`

- [ ] **Step 1: 迁 IProtocolSlave.cs + Cjt188Slave.cs**

- `IotPlugin.Cjt188/Sim/IProtocolSlave.cs`：复制 `IotSimulator.Core/Slaves/IProtocolSlave.cs`，命名空间 → `IotPlugin.Cjt188.Sim`。
- `IotPlugin.Cjt188/Sim/Cjt188Slave.cs`：复制 `IotSimulator.Core/Slaves/Cjt188Slave.cs`，命名空间 → `IotPlugin.Cjt188.Sim`；using 改 `IotDriverCore.Simulation`/`IotDriverCore`；构造 `DeviceModel device` → `SimDevice device`（字段名一致：`Address`/`MeterType`/`Points`）；编解码不改。

- [ ] **Step 2: 写 Cjt188Simulator.cs**

复制 Task 4 Step 3 的 `Dlt645Simulator.cs` 结构到 `IotPlugin.Cjt188/Sim/Cjt188Simulator.cs`，改动：
- 命名空间 → `IotPlugin.Cjt188.Sim`；类名 `Cjt188Simulator`
- `Capability`：`DefaultPort = 9188, Protocol = "cjt188"`
- `Accumulator = new FrameAccumulator(FrameAccumulator.ExtractCjt188)`
- 从站构造 `new Cjt188Slave(dev)`（无 is1997 参数）
- simId 前缀 `cjt188-`

- [ ] **Step 3: Cjt188Plugin.cs 加 ISimulatable 委托**

同 Task 4 Step 4，Simulator 换 `Sim.Cjt188Simulator`。

- [ ] **Step 4: 写测试 Cjt188SimTests.cs**

```csharp
using IotDriverCore;
using IotDriverCore.Simulation;
using IotPlugin.Cjt188.Sim;
using Xunit;

public class Cjt188SimTests
{
    [Fact]
    public async Task Cjt188Simulator_StartStop()
    {
        var sim = new Cjt188Simulator();
        var req = new SimStartRequest
        {
            Mode = SimMode.Slave, Port = 19188,
            Devices = new List<SimDevice>
            {
                new() { Address = "00000000000001", MeterType = "0x10",
                        Points = new List<SimPoint> { new() { Di = "0x1F90", Length = 4,
                            Generator = new GeneratorModel { Type = "constant", Base = 100 } } } }
            }
        };
        var status = await sim.StartSimAsync(req, default);
        Assert.True(status.Running);
        await sim.StopSimAsync(status.SimId);
        Assert.Empty(sim.ListSims());
    }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~Cjt188SimTests`
Expected: PASS（1 passed）

- [ ] **Step 6: 提交**

```bash
git add IotPlugin.Cjt188/ IotTests/Cjt188SimTests.cs
git commit -m "feat(sim): CJT188从站迁入插件并实现ISimulatable"
```

---

## Task 6: 删除 IotSimulator/IotSimulator.Core 项目并修复测试引用

**Files:**
- Delete: `IotSimulator/`（整个目录）
- Delete: `IotSimulator.Core/`（整个目录）
- Modify: `IotWebApi.sln`（移除两项目）
- Modify: `IotTests/IotTests.csproj`（移除 IotSimulator.Core 引用）
- Modify: `IotTests/FaultInjectorTests.cs`、`IotTests/SlaveRoundTripTests.cs`（改 using）
- Delete-if-orphan: `IotTests/FaultInjectorTests.cs`（若与 Task1 SimulationCoreTests 重复则删旧留新）

**Interfaces:**
- Consumes: Task 1-5 迁移后的类型（`IotDriverCore.Simulation.*`、各插件 `Sim.*`）

- [ ] **Step 1: 改 FaultInjectorTests.cs 的 using 指向新位置**

`IotTests/FaultInjectorTests.cs` 顶部 `using IotSimulator.Core.Faults;`（及 `.Scenario`）改为 `using IotDriverCore.Simulation;`。若其断言与 Task 1 `SimulationCoreTests` 完全重复，删除本文件（保留 Task 1 的），并在 Step 5 提交信息注明。

- [ ] **Step 2: 改 SlaveRoundTripTests.cs 的 using 指向各插件 Sim 命名空间**

`IotTests/SlaveRoundTripTests.cs` 顶部：
- `using IotSimulator.Core.Slaves;` → 按测试内容拆分为 `using IotPlugin.Dlt645.Sim;` / `using IotPlugin.Cjt188.Sim;`
- `using IotSimulator.Core.Scenario;` → `using IotDriverCore;`（DeviceModel→SimDevice）与 `using IotDriverCore.Simulation;`（GeneratorModel）
- 测试内 `new DeviceModel {...}` → `new SimDevice {...}`，`new PointModel {...}` → `new SimPoint {...}`（字段名一致）
- Modbus 部分若引用 `IotSimulator.Core` 的 ModbusSlave，改 `using IotPlugin.Modbus.Sim;`

- [ ] **Step 3: IotTests.csproj 移除 IotSimulator.Core 引用**

删除行：`<ProjectReference Include="..\IotSimulator.Core\IotSimulator.Core.csproj" />`

- [ ] **Step 4: sln 移除两项目**

Run:
```bash
dotnet sln IotWebApi.sln remove IotSimulator/IotSimulator.csproj
dotnet sln IotWebApi.sln remove IotSimulator.Core/IotSimulator.Core.csproj
```

- [ ] **Step 5: 物理删除两目录**

Run:
```bash
rm -rf IotSimulator IotSimulator.Core
```

- [ ] **Step 6: 全量构建 + 测试确认无悬空引用**

Run: `dotnet build IotWebApi.sln`
Expected: Build succeeded, 0 Error

Run: `dotnet test IotTests/IotTests.csproj`
Expected: PASS（所有迁移后测试绿，含 Task1-5 新测试与改指向的旧测试）

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "refactor(sim): 删除IotSimulator/IotSimulator.Core独立项目,测试引用改指向迁移后位置"
```

---

## Task 7: SimulatorController REST 层 + 点表映射

**Files:**
- Create: `IotWebApi/Areas/Device/Controllers/SimulatorController.cs`
- Test: `IotTests/SimulatorMappingTests.cs`（点表映射纯函数测试）

**Interfaces:**
- Consumes: `OperatorCommon.DicPlugins`（`ConcurrentDictionary<string, ICenBoPlugin>`）、`ISimulatable`、`DeviceInfoDAO`、`DeviceTypeParamDAO`、`SysPluginDAO`
- Produces:
  - `static class SimDeviceMapper { SimDevice Map(DeviceInfoEntity dev, List<DeviceTypeParam> points); }`
  - REST 端点：`GetDeviceSimMeta`/`StartSim`/`StopSim`/`ListSims`/`InjectFault`

- [ ] **Step 1: 写点表映射失败测试 SimulatorMappingTests.cs**

```csharp
using IotDriverCore;
using IotModel;
using IotWebApi.Areas.Device.Controllers;
using Xunit;

public class SimulatorMappingTests
{
    [Fact]
    public void Map_ModbusPoint_CopiesFuncCodeAndAddress()
    {
        var dev = new DeviceInfoEntity { DeviceAdr = 1 };
        var points = new List<DeviceTypeParam>
        {
            new() { ParamCode = "temp", ParamAddr = 40001, CollectFuncCode = 3,
                    CollectDataType = "float32", CollectRegLength = 2 }
        };
        var sim = SimDeviceMapper.Map(dev, points);
        Assert.Equal("1", sim.Address);
        Assert.Single(sim.Points);
        Assert.Equal("temp", sim.Points[0].ParamCode);
        Assert.Equal(3, sim.Points[0].FuncCode);
        Assert.Equal("40001", sim.Points[0].Di);
        Assert.Equal("float32", sim.Points[0].DataType);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~SimulatorMappingTests`
Expected: FAIL（`SimDeviceMapper` 未定义）

- [ ] **Step 3: 写 SimulatorController.cs（含 SimDeviceMapper）**

参照同目录 `DeviceControlController.cs` 的 `ControllerBaseApi` 基类、`[Route("Api/[controller]/[action]")]` 风格。核心骨架（映射器为可测静态类）：

```csharp
using CenBoCommon.Zxx;
using IotDriverCore;
using IotLog;
using IotModel;
using IotWebApi.Common;
using Microsoft.AspNetCore.Mvc;

namespace IotWebApi.Areas.Device.Controllers
{
    /// <summary>设备点表→SimDevice映射(纯函数,可单测;数据源复用平台点表,零手工配点)</summary>
    public static class SimDeviceMapper
    {
        public static SimDevice Map(DeviceInfoEntity dev, List<DeviceTypeParam> points)
        {
            return new SimDevice
            {
                Address = dev.DeviceAdr.ToString(),
                Points = points.Select(p => new SimPoint
                {
                    ParamCode = p.ParamCode,
                    Di = p.ParamAddr.ToString(),
                    FuncCode = p.CollectFuncCode,
                    Length = p.CollectFuncCode <= 2 ? 1 : Math.Max(1, p.CollectRegLength),
                    DataType = (p.CollectDataType ?? "uint16").Trim(),
                    Generator = new IotDriverCore.Simulation.GeneratorModel { Type = "random", Min = 0, Max = 100 }
                }).ToList()
            };
        }
    }

    /// <summary>设备模拟控制器(UI→REST→路由到插件ISimulatable;数据源复用平台设备/点表)</summary>
    [ApiController]
    public class SimulatorController : ControllerBaseApi
    {
        /// <summary>取设备模拟元信息:点表快照+所属插件是否支持模拟+默认端口</summary>
        [HttpPost]
        [Route("Api/[controller]/[action]")]
        public object GetDeviceSimMeta(int deviceId)
        {
            var dev = DeviceInfoDAO.Instance.GetOneBy(t => t.DeviceId == deviceId);
            if (dev == null) return new { ok = false, msg = "设备不存在" };
            var points = DeviceTypeParamDAO.Instance.GetListBy(t => t.DeviceTypeCode == dev.DeviceTypeCode
                && t.CollectFuncCode >= 1 && t.CollectFuncCode <= 4);
            var sim = ResolveSim(dev.DeviceTypeCode, out var pluginName);
            return new
            {
                ok = true,
                supportSim = sim != null,
                pluginName,
                capability = sim?.Capability,
                device = SimDeviceMapper.Map(dev, points ?? new())
            };
        }

        /// <summary>启动模拟(路由到设备所属插件)</summary>
        [HttpPost]
        [Route("Api/[controller]/[action]")]
        public async Task<object> StartSim([FromBody] StartSimBody body)
        {
            var dev = DeviceInfoDAO.Instance.GetOneBy(t => t.DeviceId == body.DeviceId);
            if (dev == null) return new { ok = false, msg = "设备不存在" };
            var sim = ResolveSim(dev.DeviceTypeCode, out _);
            if (sim == null) return new { ok = false, msg = "该设备所属插件不支持模拟" };
            var points = DeviceTypeParamDAO.Instance.GetListBy(t => t.DeviceTypeCode == dev.DeviceTypeCode
                && t.CollectFuncCode >= 1 && t.CollectFuncCode <= 4) ?? new();
            var simDev = SimDeviceMapper.Map(dev, points);
            ApplyOverrides(simDev, body);
            var req = new SimStartRequest { Mode = SimMode.Slave, Port = body.Port, Devices = new() { simDev } };
            var status = await sim.StartSimAsync(req, default);
            status.DeviceId = dev.DeviceId;
            return new { ok = true, status };
        }

        /// <summary>停止模拟</summary>
        [HttpPost]
        [Route("Api/[controller]/[action]")]
        public async Task<object> StopSim([FromBody] StopSimBody body)
        {
            foreach (var sim in AllSims()) await sim.StopSimAsync(body.SimId);
            return new { ok = true };
        }

        /// <summary>列出所有运行中模拟</summary>
        [HttpPost]
        [Route("Api/[controller]/[action]")]
        public object ListSims()
        {
            var all = AllSims().SelectMany(s => s.ListSims()).ToList();
            return new { ok = true, sims = all };
        }

        /// <summary>运行中注入/清除故障</summary>
        [HttpPost]
        [Route("Api/[controller]/[action]")]
        public async Task<object> InjectFault([FromBody] InjectFaultBody body)
        {
            foreach (var sim in AllSims())
                await sim.InjectFaultAsync(body.SimId, new SimFaultSpec { Kind = body.Kind, Probability = body.Probability, DelayMs = body.DelayMs });
            return new { ok = true };
        }

        // ===== 内部:插件解析 =====

        /// <summary>按设备类型编码找所属插件(经Manifest的addressing/DeviceTypeCodes配置),转型ISimulatable</summary>
        private static ISimulatable? ResolveSim(string deviceTypeCode, out string pluginName)
        {
            pluginName = "";
            foreach (var kv in OperatorCommon.DicPlugins)
            {
                if (kv.Value is ISimulatable sim)
                {
                    // 简化:首个支持模拟且协议匹配的插件;精确匹配可后续按DeviceTypeCodes配置细化
                    pluginName = kv.Value.PluginName;
                    return sim;
                }
            }
            return null;
        }

        private static IEnumerable<ISimulatable> AllSims() =>
            OperatorCommon.DicPlugins.Values.OfType<ISimulatable>();

        private static void ApplyOverrides(SimDevice dev, StartSimBody body)
        {
            if (body.Generators == null) return;
            foreach (var ov in body.Generators)
            {
                var p = dev.Points.FirstOrDefault(x => x.ParamCode == ov.ParamCode);
                if (p != null) p.Generator = ov.Generator;
            }
        }

        public class StartSimBody
        {
            public int DeviceId { get; set; }
            public int Port { get; set; }
            public List<GeneratorOverride>? Generators { get; set; }
        }
        public class GeneratorOverride
        {
            public string ParamCode { get; set; } = "";
            public IotDriverCore.Simulation.GeneratorModel Generator { get; set; } = new();
        }
        public class StopSimBody { public string SimId { get; set; } = ""; }
        public class InjectFaultBody
        {
            public string SimId { get; set; } = "";
            public string Kind { get; set; } = "";
            public double Probability { get; set; } = 1;
            public int DelayMs { get; set; } = 50;
        }
    }
}
```

> 注：`ResolveSim` 首版按"首个支持模拟的插件"简化。若需按设备类型精确路由到对应协议插件，读取插件 Manifest 的 `DeviceTypeCodes` 匹配——列为 Step 3 内注释的后续优化点，本任务不实现（YAGNI）。

- [ ] **Step 4: 运行映射测试确认通过**

Run: `dotnet test IotTests/IotTests.csproj --filter FullyQualifiedName~SimulatorMappingTests`
Expected: PASS（1 passed）

- [ ] **Step 5: 全量构建确认控制器编译**

Run: `dotnet build IotWebApi.sln`
Expected: Build succeeded, 0 Error

- [ ] **Step 6: 提交**

```bash
git add IotWebApi/Areas/Device/Controllers/SimulatorController.cs IotTests/SimulatorMappingTests.cs
git commit -m "feat(sim): SimulatorController REST层+设备点表到SimDevice映射"
```

---

## Task 8: DeviceInfoController 采集开关端点

**Files:**
- Modify: `IotWebApi/Areas/Device/Controllers/DeviceInfoController.cs`（加 ToggleCollection）

**Interfaces:**
- Consumes: `DeviceInfoDAO`、`ConfigReloadNotifier`（已在构造函数注入为私有字段 `_configReload`；公开方法 `void Notify(string reason)`；现有 `Insert/Update/Delete` 已有 6 处 `_configReload.Notify("...")` 调用范例）
- Produces: `POST Api/DeviceInfo/ToggleCollection { deviceId, isCollection }`

- [ ] **Step 1: 加 ToggleCollection 端点**

在 `DeviceInfoController` 类内（`Delete` 方法后，第 199 行附近）追加。字段 `_configReload`、方法 `Notify(string)` 已核实，直接照抄现有 `_configReload.Notify("设备修改")` 范例：

```csharp
        /// <summary>
        /// 启用/停用单设备采集(切IsCollection单字段+通知所属插件重建拓扑,即时生效)
        /// </summary>
        [HttpPost]
        [Route("Api/[controller]/[action]")]
        public string ToggleCollection(int deviceId, int isCollection)
        {
            try
            {
                var dev = DeviceInfoDAO.Instance.GetOneBy(t => t.DeviceId == deviceId);
                if (dev == null) return "设备不存在";
                dev.IsCollection = isCollection == 1 ? 1 : 0;
                DeviceInfoDAO.Instance.UpdateColumns(dev, it => new { it.IsCollection });
                _configReload.Notify(dev.IsCollection == 1 ? "启用设备采集" : "停用设备采集");
                return "success";
            }
            catch (Exception ex)
            {
                LogHelper.ErrorLogWrite("DeviceInfoController", "ToggleCollection", ex.ToString(), "设备管理");
                return "操作失败";
            }
        }
```

- [ ] **Step 2: 全量构建确认编译**

Run: `dotnet build IotWebApi.sln`
Expected: Build succeeded, 0 Error

- [ ] **Step 3: 提交**

```bash
git add IotWebApi/Areas/Device/Controllers/DeviceInfoController.cs
git commit -m "feat(sim): DeviceInfo新增ToggleCollection采集开关端点(切IsCollection+通知插件重建)"
```

---

## Task 9: 前端 API 封装 + 设备行双按钮

**Files:**
- Create: `vuefrontend/src/api/simulator.ts`
- Modify: `vuefrontend/src/views/iot/device/utils/hook.tsx`（操作列加双按钮 + 事件）

**Interfaces:**
- Consumes: 项目现有 http 封装（参照同级 api 文件的 `http.request` 用法）
- Produces: `getDeviceSimMeta`/`startSim`/`stopSim`/`listSims`/`injectFault`/`toggleCollection` API 函数；hook 暴露 `handleToggleCollection(row)`、`handleOpenSim(row)`

- [ ] **Step 1: 看现有 api 封装风格**

Run: `ls vuefrontend/src/api/ && head -30 vuefrontend/src/api/$(ls vuefrontend/src/api/ | grep -i device | head -1)`
Expected: 看到 http 请求封装模式（`http.request<T>("post", url, { data })`），Step 2 照此写。

- [ ] **Step 2: 写 simulator.ts（照 Step 1 风格）**

```typescript
import { http } from "@/utils/http";

const base = "/Api/Simulator";

export const getDeviceSimMeta = (deviceId: number) =>
  http.request("post", `${base}/GetDeviceSimMeta?deviceId=${deviceId}`);

export const startSim = (data: any) =>
  http.request("post", `${base}/StartSim`, { data });

export const stopSim = (simId: string) =>
  http.request("post", `${base}/StopSim`, { data: { simId } });

export const listSims = () =>
  http.request("post", `${base}/ListSims`);

export const injectFault = (data: any) =>
  http.request("post", `${base}/InjectFault`, { data });

export const toggleCollection = (deviceId: number, isCollection: number) =>
  http.request(
    "post",
    `/Api/DeviceInfo/ToggleCollection?deviceId=${deviceId}&isCollection=${isCollection}`
  );
```

> `@/utils/http` 与 `http.request` 签名以 Step 1 观察到的实际封装为准。

- [ ] **Step 3: hook.tsx 操作列加双按钮**

在 `vuefrontend/src/views/iot/device/utils/hook.tsx` 的操作列（现有编辑/删除/命令按钮所在的 `cellRenderer` 或 columns 操作项）追加两个按钮。参照现有按钮写法（`el-button` + `onClick`），新增：

- 「采集/停采」按钮：文案随 `row.IsCollection` 变（`===1` 显示"停采"，否则"启采"），`onClick` 调 `handleToggleCollection(row)`（`ElMessageBox.confirm` 二次确认 → `toggleCollection` → 刷新列表）。
- 「模拟」按钮：`onClick` 调 `handleOpenSim(row)`（打开模拟弹窗，见 Task 10）。

在 hook 顶部 `import { toggleCollection } from "@/api/simulator";`，实现两个 handler 并 return 出去供 index.vue 用。confirm/message 用项目现有 `ElMessageBox`/`message` 工具（参照本文件已有用法）。

- [ ] **Step 4: 前端构建确认无类型错误**

Run: `pnpm -C vuefrontend build`
Expected: build 成功（vite build 通过，无 TS 报错）

- [ ] **Step 5: 提交**

```bash
git add vuefrontend/src/api/simulator.ts vuefrontend/src/views/iot/device/utils/hook.tsx
git commit -m "feat(sim): 前端simulator API封装+设备行采集/模拟双按钮"
```

---

## Task 10: 模拟弹窗组件（两标签页 + SignalR 帧日志）

**Files:**
- Create: `vuefrontend/src/views/iot/device/sim-dialog.vue`
- Modify: `vuefrontend/src/views/iot/device/index.vue`（挂载弹窗，绑定 handleOpenSim）

**Interfaces:**
- Consumes: `getDeviceSimMeta`/`startSim`/`stopSim`/`listSims`/`injectFault`（Task 9）；现有 SignalR 客户端（参照 `vuefrontend/src` 内 signalr 用法）
- Produces: `<SimDialog ref=... />` 组件，暴露 `open(row)` 方法

- [ ] **Step 1: 看现有弹窗组件与 SignalR 前端用法**

Run: `head -40 vuefrontend/src/views/iot/device/command-send.vue && grep -rln "signalr\|HubConnection\|@microsoft/signalr" vuefrontend/src | head -5`
Expected: 看到 Element Plus `el-dialog` 弹窗模式 + SignalR 连接方式，Step 2/3 照此写。

- [ ] **Step 2: 写 sim-dialog.vue（标签页1:本设备模拟）**

用 `el-dialog` + `el-tabs`。标签页 1「本设备模拟」含：
- 顶部：监听端口输入（`el-input-number`，默认取 `capability.defaultPort`）、从站号/协议只读显示、运行状态灯。
- 点表表格（`el-table`）：参数/类型/地址只读列 + 生成器下拉（`el-select`：随机游走/正弦/斜坡/固定值）+ 覆盖参数输入列。
- 故障注入区（`el-checkbox`：错帧/超时丢弃/半包延迟 + 延迟输入）。
- 实时帧日志区（滚动 `div`，订阅 SignalR 帧日志事件，按 `SimLogEntry.Direction` 显示 ←/→ + hex）。
- 底部按钮：启动模拟（调 `startSim`）/停止（调 `stopSim`）/关闭。启动后按钮切换为"停止"。

`open(row)` 方法：调 `getDeviceSimMeta(row.DeviceId)` 拉点表 + capability 渲染，`dialogVisible=true`。

- [ ] **Step 3: 写 sim-dialog.vue（标签页2:全部运行中）**

标签页 2「全部运行中」：`el-table` 列出 `listSims()` 结果（设备/端口/协议/启动时间/状态灯），每行「停止」按钮 + 顶部「全部停止」。进入标签页或定时刷新调 `listSims`。

- [ ] **Step 4: index.vue 挂载弹窗**

在 `vuefrontend/src/views/iot/device/index.vue` 模板加 `<SimDialog ref="simDialogRef" />`，import 组件，在 hook 的 `handleOpenSim(row)` 里调 `simDialogRef.value.open(row)`（通过 index.vue 传入或 expose）。

- [ ] **Step 5: 前端构建确认无类型错误**

Run: `pnpm -C vuefrontend build`
Expected: build 成功

- [ ] **Step 6: 提交**

```bash
git add vuefrontend/src/views/iot/device/sim-dialog.vue vuefrontend/src/views/iot/device/index.vue
git commit -m "feat(sim): 设备模拟弹窗组件(本设备模拟+全部运行中双标签页+SignalR帧日志)"
```

---

## Task 11: 端到端联调验证与文档收口

**Files:**
- Modify: `docs/协议模拟器与插件体系完善方案.md`（追加"模拟能力内嵌插件"收口说明，若存在）
- Verify: 无新增代码文件

**Interfaces:**
- Consumes: Task 1-10 全部产出

- [ ] **Step 1: 后端全量构建 + 测试**

Run: `dotnet build IotWebApi.sln && dotnet test IotTests/IotTests.csproj`
Expected: Build 0 Error；测试全绿

- [ ] **Step 2: 前端全量构建**

Run: `pnpm -C vuefrontend build`
Expected: vite build 通过

- [ ] **Step 3: 端到端手动联调（起后端 + 前端，验证闭环）**

按 `memory/local-dev-runtime-env.md` 起本机后端（PG 6305 + Redis 8111 + RabbitMQ MQTT 1883）与前端。手动验证：
1. 设备管理页出现「采集/停采」「模拟」双按钮。
2. 点某 Modbus 设备「模拟」→ 弹窗显示该设备点表 → 设端口 → 启动 → 状态灯亮。
3. 平台的 Modbus 采集插件连上该假从站 → 监控页看到该设备上线且读到模拟值。
4. 「全部运行中」标签页看到该模拟实例，可停止。
5. 点「停采」→ 二次确认 → 该设备 `IsCollection` 变 0，采集停止。

用 Playwright MCP 或手动截图佐证（参照 memory 里 Playwright 四页验证的方式）。

- [ ] **Step 4: 文档收口**

在设计文档 `docs/superpowers/specs/2026-07-12-插件内嵌模拟能力设计.md` 末尾追加「## 8. 落地记录」，记录：实际迁移的类清单、各插件新增依赖、端到端验证结果、已知限制（OpcUa/S7 未实现模拟、Modbus 故障注入空实现）。

- [ ] **Step 5: 提交**

```bash
git add docs/
git commit -m "docs(sim): 插件内嵌模拟能力端到端联调收口与落地记录"
```

---

## 自检记录

- **Spec 覆盖**：撤两项目(Task6)、ISimulatable(Task2)、生成器/故障下沉(Task1)、三协议从站迁移(Task3/4/5)、REST层(Task7)、采集开关(Task8)、前端双按钮(Task9)、模拟弹窗两标签页(Task10)、端到端(Task11) — 全覆盖。
- **占位符**：`{字段名}`/`NotifyConfigChanged` 已显式标注"以 Step 1 grep 结果为准"，非占位符而是运行时确认点；`http.request` 以观察实际封装为准，已注明。
- **类型一致**：`ISimulatable` 五方法签名 Task2 定义、Task3/4/5/7 消费一致；`SimDevice/SimPoint` 字段名从站迁移与映射器一致（`Address/Points/ParamCode/Di/FuncCode/Length/DataType/Generator`）；`GeneratorModel/FaultModel` 命名空间 `IotDriverCore.Simulation` 全程一致。
- **风险**：FluentModbus 版本需与原项目对齐(Task3 Step1 注明)；`ResolveSim` 首版简化路由(Task7 注明后续优化点)。
